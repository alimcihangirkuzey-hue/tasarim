# TODO — Faz kapsamı dışına düşen notlar (M10)

Bu dosya, geliştirme sırasında "hazırken yapılmayan" işlerin kaydıdır.
Opus 4.8 devraldığında CONSTITUTION.md ile birlikte bu dosyayı okur.

- [x] **A-08 devir işi — KAPANDI (2026-07-26, journal `2026-07-26-a08-niteleme-turu`):** 9 canlı dosyadaki çıplak atıfların tamamı `arşiv CONSTITUTION teknoloji ADR-n` biçimine nitelendi; envanter kapanış öncesi ve sonrası bağımsız grep'le ölçüldü (kalan çıplak atıf: 0). Tek özel durum kayıtlı: `tr.json` `cmyk_missing` operatöre görünen dizeydi — niteleme görüntülenen metni değiştirdi (TODO bu dosyayı açıkça tura dahil ettiği için kapsam-içi; journal notu seq 3).

> **Ritüel:** origin/main'e KOD push'u yalnız mimar merge onayından SONRA; docs/pilot-kayıt commit'leri istisna. Koşu, mimarın "başla"sı olmadan başlamaz.
>
> **Ritüel eki (F7-A dersi):** Onaylı plan, koşu başında dala/scratchpad'e kalıcılaştırılır (oturum kaybına dayanıklılık — plan sohbette kalırsa bağlam özetlenince/oturum düşünce kaybolur; F7-A'da plan önceki oturum transkriptinden kurtarıldı).

> **K-2 ŞERHİ (2026-08-06, ürün sahibi kararı — `docs/URUN_SAHIBI_KARARLARI.md`):**
> ana modül "bitti" sayılmak için beş yeşil kapı YETMEZ; sistem ayağa
> kaldırılıp o modülün çıktısı **ekran görüntüsüyle** kanıtlanır. Kanıtlanan
> modül **kapanır** ve sıradaki üst modül tasarlanır; kapalı modüle ürün
> sahibinin açık geliştirme izni olmadan DÖNÜLMEZ. Kapanış defteri ve kapalı
> modülde ölçülüp onarılmayan yaralar kararlar defterindedir. Bu defterdeki
> açık maddeler seçilirken önce o tabloya bakılır: kapalı bir modüle dokunan
> madde, izin gelene kadar seçilemez.

### K-3 sürükle-bırak — açılan borç (2026-08-06, canvas görsel aracı paketi)

- [ ] **Canvas BASKIYA girmiyor (ölçüldü, açık — K-3'ün asıl talebi).** `canvas_json` DB'ye yazılıyor (bu turda gözle görüldü: `kind:"image", x:530, y:390, asset:ast_…`) ama render hattı onu **hiç okumuyor** — `templates/engine` ve `Template.tsx`'lerde `canvas` geçen sıfır satır. Operatör sürükleyip bırakıyor, kaydediyor, **PDF'e çıkmıyor**. Bu köprü **ADR-005'in "print/preview hattı dokunulmaz" hükmüne değer**; ayrı ve dikkatli bir paket olmalı.
- [ ] **En-boy oranı kilidi yok.** Görsel doğal oranında doğuyor (640×360 → 180×101, canlıda doğrulandı) ama Transformer serbest esnetiyor; operatör fotoğrafı ezebilir ve bunu söyleyen bir şey yok. Fotoğraf için serbest esnetme neredeyse her zaman yanlıştır.
- [ ] **Belgeden atölyeye giriş yok.** `/atolye?doc=` çalışıyor (bu turda kullanıldı) ama hiçbir ekranda bağlantı yok; operatör URL'i elle yazmadan ulaşamıyor.
- [ ] **Görsel kırpma/maskeleme yok.** Gösterilen örneklerdeki hücre düzeni (kare fotoğraf ızgarası) kırpma ister; bugün görsel kutusuna sığdırılıyor.

### K-1/A tek tık akışı — açılan borç (2026-08-06, şablon seçim sorusu paketi)

- [ ] **Şablon seçici hâlâ `window.confirm` (ölçüldü, açık).** `2026-08-06-sablon-secim-sorusu` paketi sorunun **metnini** ilandan türetti (ad manifest'ten, sıra ilandan, tür adı i18n'den) ve ikili diyalogun ön-koşulunu nöbetçiyle görünür kıldı — ama diyalogun **kendisi** hâlâ iki seçenekli bir işletim sistemi kutusu: önizleme yok ve **"İptal" turu iptal etmiyor, ikinci şablonu seçiyor**. Operatör bu noktada vazgeçemiyor. Gerçek bir seçici (küçük modal + önizleme + gerçek iptal) arayüz tasarımı işi; ayrı dilim. Üçüncü bir şablon eklendiği gün nöbetçi zaten durduracak.

### K-1/B çoklu proje hızı — açılan borç (2026-08-06, toplu aktarım gerekçesi paketi)

- [ ] **Toplu aktarım sonucu TOAST'ta gösteriliyor (ölçüldü, açık).** `2026-08-06-toplu-aktarim-gerekcesi` paketi düşen/engelli kalemin **gerekçesini üretiyor** (önceden yutuluyordu) ama sunum tek bir toast. 3 sorunlu kalemde okunur; 15'te metin duvarına döner ve muhtemelen kırpılır — `2026-08-06-hata-mesaji-siniri` paketinin ölçtüğü sınıfın aynısı. Kalıcı bir sonuç paneli (üretilen dosyalar listesinin yanında, satır satır durum) arayüz tasarımı işidir; ayrı dilim.
- [ ] **Toplu BASKI kuyruğu — K-1/B'nin üçüncü üçte biri, hâlâ yok.** Toplu tasarıma başlatma ✓ · toplu dışa aktarım ✓ · toplu baskı ✗. Baskı kuyruğunun durum modeli (kim "basıldı" der, hangi geçişler geçerli) **ürün kararı** içerir; mekanizma yazılmadan önce o karar gerekir.

### K-1/D sektörsüz dil — açılan borç (2026-08-06, doğuş kataloğu paketi)

- [ ] **ALAN ADLARI hâlâ restoran/Fransızca ilan ediyor (ölçüldü, açık).** `2026-08-06-dogus-katalogu` paketi dipnotun **değerini** kurulum ilanına bağladı; **adını** bağlamadı. Bugün bir tabelacı kurulumunda da şema `catalog.footnote_fr` ve `client.menu_language` diyor; ikincisi bir **müşteri** alanı olduğu hâlde arayüzde "Menü dili" diye etiketleniyor (`i18n/tr.json:35`) ve `PageChrome` dışında dijital menü ile intake'i de sürüyor. Ad değişikliği **migration ister (v13)** ve doğuş paketinin risk sınıfının dışındadır — ayrı paket, ayrı ürün-sahibi teyidi (geri döndürülemez veri emsali). Bu satır kapanana kadar D başlığının şema yarısı açıktır.
- [x] ~~**Yeni müşterinin VARSAYILAN para birimi hâlâ `EUR`.**~~ **KAPANDI (2026-08-06, journal `2026-08-06-dogus-varsayilanlari`):** `TEZGAH_PARA_BIRIMI` + `TEZGAH_CIKTI_DILI` kurulum ilanları açıldı — tanımsızken bugünkü değerler birebir (EUR/fr), ilan edilince yeni müşteri o değerlerle doğar, istek gövdesi ilanı **ezer** (ihracat müşterisi euro yazabilir), tanınmayan değerde sunucu **ayağa kalkmaz**. **BU SATIRDAKİ TARİF YANLIŞTI ve düzeltildi:** "kurulum ilanına bağla, `dogusKatalogu` emsali" diyordu; oysa `TEZGAH_IS_KOLLARI` bir **meslek** ilanıdır, ülke/para ilanı değil — Fransa'daki bir tabelacı da `tabelaci`dır ve euro yazar. Meslekten para birimi türetmek ölçülmemiş bir olguyu kod hâline getirirdi; dipnotta bağ gerçekti (menü dipnotu menü işine ait), burada yok. Bu yüzden ayrı, kimlik-bilgisi-kapılı iki ilan açıldı.
- [ ] **Dipnotun İÇERİĞİ ürün sahibinde.** Menü üretmeyen kurulumda dipnot artık **boş** doğuyor — bilerek: bir tabelacının/matbaacının dipnotunda ne yazacağı içerik kararıdır ve K-1'de açıkça açık bırakıldı. Karar geldiği gün tek satır veri.

### C-P1 sonrası açılan borç (2026-07-23, kimlik bağlama paketi)
- [x] **`@tezgah/templates` manifest-only subpath çıkarımı — KAPANDI (C-P2, 2026-07-23, journal `2026-07-23-c-p2-identity-subpath`).** `./identity` subpath'i kuruldu; `apps/server` artık `@tezgah/templates/identity` import eder, react/jsx-runtime **tamamen** server modül grafiğinin dışında (statik kanıt: esbuild metafile analizi — identity graf'i 12 dosya/0 react/0 .tsx; tam registry kontrastı 152/6/11). `registry-core.ts` invaryantın PAYLAŞILAN çekirdeği (index.ts + identity/index.ts aynı kaynağı kullanır). **Kalan bilinçli sınır (backlog DEĞİL, kayıtlı karar):** fabrika/generated manifest'leri identity kaydına GİRMEDİ — fabrika HEP `type:"menu"` beyan ettiği için (C-P0 C-B-1) iki yönlendirme yardımcısı zaten "menu"yu null'a çözüyor; kayıtlı/kayıtsız fark etmiyor. Bu varsayım `identity/index.test.ts`'teki yüksek sesli nöbetçiyle kilitli — fabrika emit'i bir gün değişirse (ürün sahibi yetkisi ister) o test PATLAR.

### Şiddet katmanı sonrası açılan borçlar (2026-07-26, kalite-şiddet paketi)
- [x] **BLOCKER enforcement — KAPANDI (2026-07-26, ürün sahibi onayı; journal `2026-07-26-blocker-enforcement`).** `empty-required` + `overflow-strategy-violation` blocker'a yükseldi ve enforcement aynı pakette geldi (4.7 tablosu birebir: **istisna verilemez** — kayıtlı onay WARNING'in mekanizmasıdır, blocker'ın değil): export modalı engelleri ayrı listeler ve blocker varken export butonu HİÇ ÇİZİLMEZ; sunucu export rotası ilk kapı olarak 409 `blocker_present` backstop'u taşır (browser'dan önce, inject-testli). Nöbetçi test artık kümenin TAMAMINI çiviler ({empty-required, overflow-strategy-violation}) — değiştirmek yeni enforcement + ürün kararı ister. Kayıtlı sınır: sunucu tam analiz KOŞAMAZ (tam registry react taşır, C-P2 kazanımı geri alınmaz); backstop istemcinin dürüst bildirimine karşı UI-hata savunmasıdır, çok-kullanıcılı model D fazı kararı.
- [x] **Profil-katmanı şiddet override'ı — KAPANDI (2026-07-26, journal `2026-07-26-profil-siddet-override`).** 4.5'in ilk iki katı kuruldu: çekirdek sözlük + manifest `severity_overrides` (7.2 "Kalite kapıları kümesi" alanı). MONOTONLUK yük-zamanı zorlanır: profil çekirdeği yalnız SIKILAŞTIRABİLİR (info<warning<blocker; gevşetme VE çekirdeği tekrarlayan ölü ilan registry kurulurken reddedilir) — 4.7 çekirdeği asgari güvence, profil verisiyle delinemez. Tüm tüketiciler profil-farkında (parametre ZORUNLU — sessiz çekirdeğe düşüş yok): editör paneli + export modalı, sunucu 409 backstop'u (`severityOverridesOf`, kayıtsız/fabrika id → çekirdek), GT-2 galerisi. v1 canlı katman: tekstil profili `mono-suggest`'i info→warning sıkılaştırır. Kayıt-defteri-geneli nöbetçi (profil-siddet.test.ts) TÜM override tablolarını çiviler. **Kalan bilinçli sınırlar (backlog değil, kayıt):** (a) ~~blocker-düzeyi profil override'ı ürün sahibi kapısında~~ **KAPANDI (2026-07-26, ürün sahibi onayı; journal `2026-07-26-low-dpi-tekstil-blocker`):** `low-dpi@tekstil → blocker` canlı — vaat ölçüldü: enforcement koduna dokunulmadı, garment manifest tablosuna eklenen satır modalda butonu kaldırdı ve sunucuda 409'a dönüştü (inject-testli); diğer ailelerde low-dpi çekirdek warning kaldı; blocker-düzeyi override'lar artık ayrı nöbetçiyle çivili; (b) payload-koşullu override (ör. low-dpi yalnız `level:"red"` iken) v2 mekanizma adayı — garment yalnız red ürettiği için bugün gerek yok; (c) 4.5'in ülke/tenant katmanları ihtiyaç doğunca.

### Sipariş şeması sonrası açılan borçlar (2026-07-26, fiyatlandırma-girdileri paketi)
- [x] ~~**Sipariş→belge köprüsü ilanı.**~~ **KAPANDI (2026-07-26; journal `2026-07-26-siparis-belge-koprusu`):** `SIPARIS_SABLONLARI` + `SIPARIS_MATERYALI` + `PARAM_AKISI` identity katmanında exhaustive ilan; `dogrulaSiparisKoprusu` yük-zamanı tutarlılığı (kayıtsız şablon / materyal uyuşmazlığı / tasarlanamaz türe seçenek → fail-loud) — ProductType↔MaterialType bağı ilk kez makine-denetimli. ProjectsPanel okuyucu oldu; davranış birebir (180 kombinasyonluk param diferansiyeli). Kalan şerh: varsayılan ölçü/alan değerlerinin manifest'e taşınması (siparis_varsayilanlari) ayrı paket adayı.
- [ ] **Opsiyonel şema alanları forma bağlı değil (kalan):** ~~print_qty~~ **print_qty BAĞLANDI (2026-07-26, ürün sahibi onayı; journal `2026-07-26-print-qty-girdisi`): baskı dörtlüsünde OPSİYONEL tiraj girdisi — toplanır/özette görünür, fiyat modeli gelmeden duruma-geçiş kapısına girmez (zorunluya yükseltme = tek satır veri, o gün ürün kararı).** `lumineux` (ışıklı tabela) ve `areas` hâlâ bağsız — forma almak ayrı ürün kararı. **DÜZELTME (2026-07-26, journal `2026-07-26-kosullu-gereklilik-v2`):** buradaki "koşullu düzey v2 ile birlikte" beklentisi ÖLÇÜMLE geçersizleşti — koşul mekanizması SLOT katmanında açıldı ama sipariş katmanına GELMEDİ: `PRICING_INPUTS`'ta 20 ilan var ve koşullu örneği SIFIR; üçüncü düzey bugün örneği olmayan ilan (ölü sözleşme) olurdu. Ayrıca özne evrenleri kesişmiyor (slot: `doc.params`+overrides+varlıklar; sipariş: `OrderItemLike`), çıktılar ayrı (çekirdek BLOCKER `empty-required` vs `missingFields`/409) ve paket yönü tek yönlü (`shared`, templates'i import etmez). Bu alanlar forma bağlanırken koşul gerekirse, o gün kendi dar çözümü kurulur.

### Önizleme paketi sonrası açılan borçlar (2026-07-26, onizleme-turleri keşfi)
- [x] ~~**`/render` ucu kanal bekçisiz (keşif bulgusu).**~~ **KAPANDI (2026-07-26; merge `e5efb8e`, journal `2026-07-26-render-kanal-bekcisi`):** `productionChannelsOf` bekçisi `/render`'a taşındı — imzalı dış istemci profil ilanının dışına çıkamaz (garment+print/preview → 400 `channel_not_declared`; browser'dan ÖNCE, inject-testli; contract sürümü korunarak). **Aynı deliğin KAYITSIZ-ID yarısı bu paketle kapandı (entegrasyon sınırı turu, journal `2026-07-26-entegrasyon-siniri-karari`):** ilk bekçi `null`'da GEÇİRİYORDU (atölyenin fabrika düşme desenini taklit ederek) — yani ilan denetiminden hiç geçmemiş bir profile (generated/`kabul-fabrika`) imzalı dış istemci üretim yaptırabiliyordu. Artık uç `disaAcikKanallar` okur (ilan ∩ contract enum) ve kayıtsız profil 400 `profile_not_registered` alır; reddedilen kanal yanıtı additive `disa_acik` alanı taşır — eski yanıt garment reddinde yalnız `declared:["png","broderie"]` diyerek dış istemciye YANLIŞ sınır bildiriyordu (ikisi de dış kanal DEĞİL). ATÖLYE/MAKİNE ASİMETRİSİ bilinçli ve kayıtlı: `exports.ts`'in null→geçer davranışı DEĞİŞMEDİ (atölyede operatör sorumludur), sıkılaşan yalnız sözleşmeli dış uçtur (9.6/703). `RENDER_CONTRACT_V=1` korunur (yeni hata kodu + additive detail alanı = additive guard, CD1-2 emsali). Kalan denetim-izi borcu aşağıda ayrı şerhtedir.
- [ ] **Şerhler (küçük temizlik/karar) — 2026-08-06'da TEK TEK ÖLÇÜLDÜ, üçü kapanmış çıktı.** Defter, kapanmış borcu açık gösterdiği sürece sıralamayı yanlış besler; her madde artık ölçüm sonucuyla yazılı:
  - ~~`api.digitalMenuHistory` ölü istemci fonksiyonu~~ **KAPANDI** — `DocumentsPanel.tsx:142` onu okuyor (dijital menü geçmişi listesi); ayrıca `api-olu-uye.test.ts` nöbetçisi bugün ölü üye bırakmıyor. Yani bu satır, YEŞİL bir testin çürüttüğü bir iddiaydı.
  - ~~PasteBox parse önizlemesi kalem özetini İKİNCİ KEZ elle yazar~~ **KAPANDI** — önizleme `itemSummary`'yi çağırıyor (`ProjectsPanel.tsx:368`); kapanışın gerekçesi kodun kendi yorumunda sayılı (technique canlı yaraydı, substrat ilandan türetilen bilgi, print_qty latent).
  - ProjectsPanel select seçenekleri sert kodlu → **KISMEN KAPANDI**: `mode` ve `technique` artık İLANDAN türer (`siparisTeknikleri`; ölçüldü — vitrophanie `[impression, decoupe]`, tişört/önlük `[impression, broderie]`, ikisi de `OrderDetailsSchema`'nın kabul ettiği kümeyle birebir). **`side` bilerek sert kodlu KALIR** ve gerekçesi kodda yazılı: uygulama yönü bir ÜRETİM KANALI değildir, `KANAL_GEREKTIRIR` zincirinde karşılığı yoktur — türetecek ilan yok. İlan doğduğu gün bağlanır.
  - EditorPage'de `exportRouteOf` çifte hesap (mutation + modal) → **AÇIK ama AYRIŞAMAZ (ölçüldü)**: iki çağrı da AYNI fonksiyonu AYNI argümanlarla (`entry.manifest.production_channels`, `doc.params.mode`) çağırıyor; modal yalnız `entry` varken çizildiği için mutation'ın `?? ["print"]` yedeği de devreye girmiyor. Yani bugün bir davranış farkı ÜRETEMEZ — temizlik kalemidir, yara değil.
  - sahne `kind` × şablon türü çapraz doğrulaması yok (garment sahnesine cam belgesi bindirilebilir) → **AÇIK, ÜRÜN KARARI** — ölçüldü: `mockup.ts` sahne türü ile belge ailesini hiç karşılaştırmıyor. Hangi düzeyde (engel mi uyarı mı) doğacağı ürün sahibinin kararıdır (yanlış-rol uyarısı v2 emsali).

### Malzeme cinsi (production_substrate) sonrası şerhler (2026-07-26, substrat paketi)
- [x] **(1) Sipariş katmanına bağlama — KAPANDI, ölçüm tasarımı DÜZELTTİ (2026-07-26, journal `2026-07-26-siparis-substrat-bagi`).** Önerilen "opsiyonel substrat girdisi" EKLENMEDİ: türetilen küme (SIPARIS_SABLONLARI × production_substrate) her tasarlanabilir türde TEK elemanlı ölçüldü — girdi bilgi taşımaz (preview_types emsali: ölü sözleşme yasak; serbest 5'li select ilanla çelişebilirdi, türetilmiş select tek seçenekli rozet olurdu). Bunun yerine sipariş katmanı substratı İLANDAN OKUR: identity'de yük-zamanı türetilmiş `SIPARIS_SUBSTRATLARI` + `siparisSubstratlari` (el yazımı ikinci kaynak yok — çift-kaynak sürüklenemez), kalem kartı özetinde ticari bilgi (3.1/202). **Girdinin eklenme koşulu nöbetçide:** siparis-koprusu.test.ts HOMOJENLİK NÖBETÇİSİ küme çok-elemanlı olduğu gün kırılır ve print_qty emsaliyle `PRICING_INPUTS` paketi açılır (o gün `PRODUCTION_SUBSTRATES` sözlüğünün shared'a taşınma kararı da yeniden değerlendirilir — bağımlılık yönü templates→shared).
- [ ] **(2) Çift-kaynak kararı — brief `print_material` serbest metni vs tipli substrat ilanı:** aynı bilginin iki evi doğdu (serbest metin brief spec_values'ta, tipli ilan manifest'te); delivery_deadline çift-kaynak bekçisi emsaliyle tek-kaynak/türetme kararı gerekir — F1 P5'in spec_values yazma bekçisine bağlı, o hatta ele alınır.
- [ ] **(3) Substrat-koşullu şiddet GEVŞETME monotonluğa takılır:** vinil geniş-format dpi gevşetmesi (uzaktan bakılan büyük yüzeyde düşük dpi kabul edilebilir) bugünkü "profil çekirdeği yalnız SIKILAŞTIRABİLİR" kuralınca registry'de reddedilir — bu ihtiyaç, yukarıdaki şiddet katmanı kaydının (b) payload-koşullu override v2 mekanizmasının müşterisidir; v2 açılırsa ilk vaka adayı.

### Hedef sektör (HEDEF_SEKTOR) sonrası şerhler (2026-07-26, sektör paketi)
- [ ] **(1) "Hedef kullanıcı" yarısı ilan dışı (kayıtlı karar):** 7.2/495 kaleminin "hedef sektör" yarısı ilan edildi (SEKTORLER + HEDEF_SEKTOR), "hedef kullanıcı" yarısı EDİLMEDİ — 1.3/95 rolleri profil-başına FARKLILAŞMIYOR (her ailede aynı roller), alan bilgi taşımazdı (preview_types emsali: ölü sözleşme yasak). Roller profil-başına farklılaştığı gün ayrı ilan kararı açılır.
- [ ] **(2) Müşteri sektörü KALICI DEĞİL (keşif bulgusu):** ClientDTO/intake commit sektör TAŞIMIYOR — paket seçimi yalnız localStorage taslağında (intakeStore) yaşıyor, commit'te müşteriye yazılmıyor. Galeri filtrelemenin (7.1/481 "kullanıcı yalnızca yaptığı işi görür" görünürlüğü) ve sipariş menü çatalının (grid|liste `window.confirm` — ProjectsPanel) sektörle çözümü bu önkoşula bağlı: müşteri sektörü kalıcı alan + migration ister; 7.1 entitlement/görünürlük katmanı ayrı paket.
- [ ] **(3) SectorPack.id kapalı union değil (sector.ts `z.string`):** derleyici-denetimli sektör kimliği F7 hattı kararı — bu pakette dokunulmadı. Şerh düşüldü: SECTOR_PACKS (MÜŞTERİNİN gastronomi kataloğu — kebap/pizza/lokanta/café/pastane) ile SEKTORLER (TEZGÂH'ın İŞ KOLU ilanı) AYRI boyutlardır; sözcük dağarcıkları örtüşmüyor (enseigne/garment/vitro SECTOR_PACKS'te karşılıksız — keşif ölçümü), biri ötekine bağlanmaz.

### Dosya gereksinimleri (gereklilik) sonrası şerhler (2026-07-26, dosya paketi)
- [ ] **(1) Koşullu gereklilik v2 — vitrophanie KAPANDI, garment AÇIK (2026-07-26, journal `2026-07-26-kosullu-gereklilik-v2`).** Borç "iki aile birlikte" diye yazılmıştı; ölçüm kapsamı DARALTTI ve iki müşteriyi AYIRDI.
  - **vitrophanie KAPANDI:** zorunluluk artık İLANDADIR — `SlotDef.gereklilik` koşullu biçimi (`{ kosul: { kind:"param_esittir", param:"mode", deger:… } }`) `vitrophanie/manifest.ts` SLOT_DEFS'e yazıldı, `index.tsx`'teki iki elle `if` jenerik motora (`eksikZorunluVarliklar`) döndü. Bu aileyi `param_esittir` %100 kapsar çünkü koşul TEK `mode` paramına bakar, param KAPALI KÜME ve iki dal TAM BÖLÜMDÜR (impression→logo, decoupe→logo_mono; üçüncü hâl yok). **TEK İLAN NOKTASI ÜÇ MANİFESTİ kapattı** — bandeau/centre/colonne ortak `SLOT_DEFS`'i `makeManifest` üzerinden paylaşır (substrat ilanının tek-nokta emsali). Uyarı dizisi BİREBİR korundu (tür + slotId + sıra); `vitrophanie/analyze.test.ts` değiştirilmeden yeşil.
  - **garment AÇIK ve engeli KOŞUL DİLİ DEĞİL — üç ÖN KOŞUL borcu ölçüldü:** (i) `areas` paramı `garment/manifest.ts` params listesinde **YOK** — alan seçimini EditorPage sert-kodlu paneli yazar (aynı yara TODO:21'de zaten kayıtlı: `areas` forma/şemaya bağlı değil); (ii) `area:*:logo` slotları `manifest.slots`'ta **YOK** (manifest yalnız `logo`/`logo_mono` ilan eder) — koşul motoru manifest üzerinden gezerken garment'ın GERÇEK slotlarını adlandıramaz; (iii) `paramValue()` **color/number paramlarında DEJENERE** — `options` boş olduğunda belge değerini ATLAR ve HER ZAMAN varsayılanı döndürür, yani `fabric_color`'ı siyah bir belgede bile `#FFFFFF` çözer ve `fabricDark` koşulu ASLA ateşlenmez. Bu üçü kapanmadan garment koşulu bildirimsel ifade edilse bile **DOĞRULANAMAZ** — ölçüm: `param_esittir` biçimi garment'ı %0 kapsıyor (override eşitliği · luminance-türevi boolean · kardeş slot varlığı · iki param bileşimi + boş-küme fallback · slot-id şablonu).
  - **"Sipariş şemasıyla AYNI v2 ailesi" cümlesi ÖLÇÜMLE DÜZELTİLDİ (eski metnin iddiası YANLIŞLANDI):** `PRICING_INPUTS`'ta **20 ilan** var ve KOŞULLU örneği **SIFIR** (yalnız `zorunlu: true|false`) — üçüncü bir düzey bugün ÖRNEĞİ OLMAYAN İLAN, yani ölü sözleşme olurdu ve deponun kendi red ölçütüne takılırdı (preview_types / entegrasyon alanı emsali). Üstelik iki borç aynı aile değil: **özne evrenleri kesişmiyor** (slot koşulu `doc.params` + `overrides` + varlıklar üzerinde; sipariş koşulu `OrderItemLike` üzerinde), **çıktılar ayrı** (çekirdek BLOCKER `empty-required` vs `missingFields`/409 duruma-geçiş kapısı), **paket yönü tek yönlü** (`shared` templates'i import ETMEZ). Sipariş "koşullu" düzeyi ilk GERÇEK örneği doğunca ayrı paket olarak açılır.
- [x] ~~**(2) Rol ilanı ayrı paket — slot hangi asset kind'ını kabul eder.**~~ **KAPANDI (2026-07-26, journal `2026-07-26-dosya-rolleri-ilani`):** `gereklilik`'in ("boş kalabilir mi") yanına rol ekseni ("NE ile doldurulur") kuruldu — `kabulEdilenTurler(slot)` kabul kümesini ÖNCE `bind`'den TÜRETİR (`brand.logo_primary|logo_mono → ["logo"]`, `item.photo → ["photo","other"]`), bind'siz slot kendi `kabul` ilanına düşer, tanınmayan bağ kısıtsız kalır. **Türetim ölçümü 18/22:** kayıt defterindeki 22 image slotunun 15'i marka logosuna, 3'ü ürün fotoğrafına bağlı — el yazımı ikinci kaynak YAZILMADAN kapandı. Kalan 4'te (`deco1-3` + trifold `cover_photo`) `bind:null` olduğu için rol TÜRETİLEMEZ, manifest ELLE ilan eder (`kabul: ["photo","other"]` — dekor/kapak marka logosu değildir) → kapsama 22/22, ikinci kaynak riski yalnız türetilemeyen 4 slotta ve orada tek kaynak manifesttir. **Kapanan yara:** AssetPicker'ın tek yönlü `excludeLogos` çentiği (yalnız "logo hariç" diyebiliyordu, "yalnız logo" diyemiyordu) INCLUDE eksenli `kabul` propuna döndü; ölçüm 0/22 rol denetiminden — **17 sabit image slotu FİLTRESİZ ve SESSİZ** idi — ilandan okunan denetime geçti. En ağır nokta marka kiti seçicileriydi (BrandKitPanel `logo_primary`/`logo_mono`): korumasızdı ve zehirlenirse `bind` üzerinden 15 slotu birden yanlış içerikle dolduruyordu (override bile gerekmeden). Davranış-birebir dallar: ürün fotoğrafı (SlotPanel) ve sahne zemini (ScenesPanel) `excludeLogos ≡ ["photo","other"]`; garment `area:*:logo` dalı zaten yapısal rol-güvenliydi (asset id değil varyant dizesi seçer) — dokunulmadı. Boş sonuç artık SESSİZ DEĞİL: filtre eledi ise seçici gerekçeyi yazar (`assets.none_accepted`).
- [ ] **(3) Brief `file_requirements` ↔ şablon `slots` köprüsü YOK (F1 hattı):** iki kopuk dosya-gereksinimi dünyası yaşıyor — brief tarafı ROL-eksenli (`f1-spec.ts file_requirements`: role "logo"/"tasarim"; tamlık f1-completeness.ts'te rolden sayılır), şablon tarafı SLOT-eksenli (SlotDef.gereklilik; empty-required slotId taşır). `AssetDTO` role TAŞIMADIĞI için brief'te "logo" rolüyle yüklenen dosya editörde slot'a otomatik bağlanamıyor; aynı gereksinim iki sözlükte iki kez tarif ediliyor ve sürüklenebilir. Tek-kaynak/türetme kararı F1 hattının işi (brief `print_material` çift-kaynak şerhi emsali — substrat paketi (2)).
- [ ] **(4) Repeater photo eksikliği uyarı sınıfına GİRMİYOR (kayıtlı mevcut davranış):** `repeater.itemSlots`'taki `photo` image slotu gereklilik ilanının dışında — ürün fotoğrafı eksikliği `missingPhotoItems` AYRI yolundan akar (binding.ts §8.2; EditorPage "fotoğraf bekleniyor" paneli) ve blocker DEĞİLDİR (Eksik Görsel Akışı: fotoğrafsız ürün yer tutucuyla basılabilir). Bu pakette BİLİNÇLİ korundu; itemSlots'a gereklilik taşımak (fotoğrafsız ürünü export engeline çevirmek) ürün kararıdır.
- [x] **(5) `paramValue()` color/number DEJENERASYONU — KAPANDI** (journal `2026-07-27-param-gecerli-deger-ilani`). Bu maddenin son cümlesi paketin ne getirmesi gerektiğini doğru saptamıştı ("asıl borç düzeltme değil TANIM: `options` yokken *geçerli değer* hiç ilan edilmemiş… ve `params.test.ts` YOKTUR"). Çözüm o teşhisi izledi ama bir yerini düzeltti: **tanım zaten vardı, okuyan yoktu** — `min`/`max`/`step` 13 number paramın 13'ünde de ilanlı, hex için `HexColor` kanonik; eksik olan bu ilanları uygulama noktasına bağlamaktı. Aşağıdaki tarihsel kayıt olduğu gibi bırakılmıştır (ölçüm izi):
- [ ] ~~**(5) `paramValue()` color/number DEJENERASYONU (koşullu-v2 keşif bulgusu — SESSİZCE YANLIŞ DEĞER):**~~ `engine/params.ts` belge değerini yalnız `opts.some((o) => o === raw)` geçerse kabul eder; `color` ve `number` paramlarında `options` YOKTUR (boş dizi) → belge değeri HER ZAMAN elenir ve fonksiyon HER ZAMAN manifest varsayılanını döndürür. Yani `fabric_color`'ı siyah yazılmış bir garment belgesinde bile `#FFFFFF` çözülür; kapsam `color` + `number` ilanlarının TAMAMIDIR (`fabric_color`, `cut_color`, vitro/enseigne `w_cm`/`h_cm`, beş ailedeki `designSeed`). **Neden bugüne kadar patlamadı — ölçüldü:** canlı `paramValue()` çağrılarının hepsi `choice`/`toggle` paramlara gidiyor (`cols`, `columns`, `flow`, `showDesc`, `qrSource`, `stampCount` — `stampCount` `options:[8,10,12]` ile CHOICE'tur, dejenerasyonun dışında); tek jenerik çağıran EditorPage param paneli `val`'ı hesaplar ama `number`/`color` dallarında KULLANMAZ (`doc.params[p.id] ?? p.default` okur, doğru davranır), ölçüler ise zod şemalarından (`VitroParamsSchema`/`TabelaParamsSchema`) geçer. Yani bu **sessizce yanlış cevap veren, bugün müşterisi olmayan bir tuzaktır** — ve garment koşulu onun İLK gerçek müşterisi olacağı için (1)'in üç ön koşulundan biridir. Düzeltmek yine de DAVRANIŞ DEĞİŞİKLİĞİ riski taşır (hep-varsayılan cevaba dayanan tüketiciler `val`'ı bugün kullanmıyor olsa da tam çağıran taraması + diferansiyel kanıt ister) → ölçülmeden DOKUNULMADI; ayrı paket adayı. **ÖLÇÜM GÜNCELLEMESİ (2026-07-26, journal `2026-07-26-uyari-kaynagi-durustlugu`):** yukarıdaki "canlı çağrıların hepsi choice/toggle'a gidiyor" cümlesi KISMEN YANLIŞ çıktı — `EditorPage.tsx:425` jenerik döngüsü `entry.manifest.params.map` ile **TÜM paramları** `paramValue`'ya geçirir (`fabric_color`, `cut_color`, `w_cm`, `h_cm`, beş ailedeki `designSeed` dahil), yani dejenere kod yolu **fiilen yürütülür**. Ama sonucu (`val`) color/number dallarında **kullanılmaz** (input `doc.params[p.id] ?? p.default` okur) → **tüketici SIFIR**. Sonuç: düzeltme bugün **hiçbir gözlemlenebilir davranışı değiştirmez** (16 canlı + 14 baseline çağrının hiçbirinde çıktı farkı yok, test kırılmaz). **Kökeni de ölçüldü:** fonksiyon `ffe2da1`'de `choice|toggle` kapalı evreninde DOĞRU yazılmış; `1cb3c9c` union'a `number|color` eklerken `engine/params.ts` güncellenmemiş — sessiz tip gevşemesi (exhaustive switch olsaydı derlemede patlardı). **Asıl borç düzeltme değil TANIM:** `options` yokken "geçerli değer" hiç ilan edilmemiş (`number` için doğal kapı `min`/`max`/`step`, `color` için hex regex) ve **`params.test.ts` YOKTUR** — yani dejenerasyonu pinleyen test de, düzeltmeyi koruyacak nöbetçi de yok. Paket bu tanımı + testi birlikte getirmeli.

### Rol ilanı (kabul kümesi) sonrası şerhler (2026-07-26, rol paketi)
- [ ] **(a) YANLIŞ ROL UYARISI v2 — filtre ÖNLEYİCİDİR, DENETÇİ DEĞİLDİR:** `kabul` yalnız KAYNAKTA engeller (seçici yanlış türü göstermez). Şu iki vakayı **TESPİT ETMEZ**: (i) API/override yoluyla zorla bağlanmış varlık (`overrides[slotId] = assetId` doğrudan yazılabilir — picker tek giriş yolu değil), (ii) filtre yokken GEÇMİŞTE bağlanmış yanlış varlık (mevcut belgeler taranmıyor; kapanan yara ileriye dönüktür, geriye dönük değil). Karşılığı bir uyarı türüdür (`wrong-asset-kind`) ve ucuz değildir: uyarı sözlüğü + jenerik üretici (eksikZorunluVarliklar emsali) + şiddet çekirdeği + profil override tablosu + editör paneli + export modalı + sunucu 409 backstop'u + nöbetçi test ≈ **8 dosya**, üstüne 4.7 blocker disiplini (blocker ise "istisna verilemez", warning ise kayıtlı onay mekanizması) ve **ürün sahibi onayı** — hangi düzeyde doğacağı ürün kararıdır. Bu pakette bilerek açılmadı.
- [ ] **(b) `deco1-3` slotları UI'dan DOLDURULAMIYOR (keşif bulgusu, rol ilanından BAĞIMSIZ yara):** menu-liste-premium'un üç dekor slotu ilan edilmiş durumda ama **boş dekor slotu hiç çizilmiyor** — `analyze.ts` yalnız `if (asset) decor.push(...)` yapıyor, Template dekor bandını `a.decor.length > 0` koşuluyla çiziyor. Yani boş slot tıklanamaz → seçilemez → SlotPanel açılmaz → varlık bağlanamaz. Rol ilanı artık bu slotları da kapsıyor (manifest `kabul: ["photo","other"]` ilan ediyor), ama **slot fiilen ÖLÜ** — yani bugün o ilanın tek okuyucusu olan seçici HİÇ AÇILMIYOR: doldurmanın tek yolu override'ı elle yazmak. Çözüm ya boş yer tutucu çizmek (edit modunda hayalet kutu — dekor bandı yüksekliği bugün 0) ya da slotları geri çekmek (`balanceLastColumn` emsali: kimsenin geçiremediği ilan ölü sözleşmedir); ikisi de görsel karar → ürün sahibi.
- [x] **(c) `clientAssets()` brief_files filtresi — KAPANDI (2026-07-26, journal `2026-07-26-brief-varlik-sinirlari`).** Aynı kural iki uçta farklı uygulanıyordu: `/api/assets/common` brief yüklemelerini eliyordu, `routes/clients.ts clientAssets()` elemiyordu — brief-files asset'i `brief.customer_ref` ile yazdığı için üretim girdisi müşteri galerisine ve AssetPicker'ın "Müşteri" sekmesine düşüyordu (üstelik brief yüklemesi thumb üretmediğinden **kırık görsel** olarak). Kanonik 6.3/440 ihlali kapandı: aynı `NOT IN (SELECT asset_id FROM brief_files)` dizesi müşteri havuzuna da uygulandı. **Ölçülen endişe gerçekleşmedi:** `assetById` bu listeden değil `ClientDTO.assets`'ten okur ve brief varlığı bir slota BAĞLI DEĞİLDİR (brief dosyaları editör slotlarına hiç girmiyor) — kopma olmadı, regresyon nöbetçisi filtrenin dar kaldığını çiviliyor (`brief-varlik-sinirlari.test.ts`: brief_files satırı kalkınca varlık yine görünür).
- [x] **(d) `scanUsages` brief_files taraması — KAPANDI (2026-07-26, journal `2026-07-26-brief-varlik-sinirlari`).** Kullanım taraması dört yere bakıp `brief_files`'a bakmadığı için `DELETE /api/assets/:id` serbest görüyor, FK `ON DELETE CASCADE` brief dosyası satırını yok ediyor ve F1 tamlığı **audit satırı olmadan** geriliyordu (4.7/354 append-only denetim izi ile çelişir). Taramaya brief dalı eklendi: silme artık 409 döner ve kullanım listesinde `brief dosyası — <rol> v<n> (<brief id>)` görünür. **F1 hattına bırakılan karar** (brief kapanmışsa dosyası silinebilir mi) bu paketle KAPANMADI — bugünkü duruş korumacıdır: silmek isteyen önce brief'te dosyayı geçersizler (o yol `file_invalidated` audit'i yazar), sonra siler.

### Entegrasyon sınırı (7.2/507) sonrası şerhler (2026-07-26)
- [ ] **(1) Profil-başına "entegrasyon" manifest alanı ÖLÇÜMLE REDDEDİLDİ (kayıtlı karar, backlog DEĞİL):** 7.2/507'nin makine-okunur karşılığı için önerilen manifest alanı üç ölçümde düştü — (a) TÜRETİLEBİLİR: dışa açık kanal kümesi zaten `production_channels ∩ DIS_KANALLAR`, el yazımı alan İKİNCİ KAYNAK olur ve sürüklenir; (b) FARKLILAŞMIYOR: 11 ailenin tamamı aynı şeyi söylerdi (menu/flyer/kart/cam/tabela → `["print","preview"]`, garment → `[]`) — ayırt eden bilgi yok (preview_types emsali: ölü sözleşme yasak); (c) YER: kanonikte entegrasyon 7.3/7.4'e verilmiş, 7.2 profil kimliğinin alanı değil. Bunun yerine türetilmiş sorgu (`disaAcikKanallar`) + tek sözlük (`DIS_KANALLAR`) kuruldu. **Yeniden değerlendirme koşulu:** profil-başına FARKLILAŞAN ikinci bir modül bağı doğarsa (ör. bir ailenin dış uca farklı yetenek/limit ilan etmesi gerekirse) alan kararı yeniden açılır.
- [x] ~~**(2) DENETİM İZİ borcu (karşılıksız kanonik madde):** `/render` STATELESS'tir (T3 PART-B kararı — `export_records`'a yazmaz; provenance yalnız yanıt meta'sı + dosya adı)…~~ **KAPANDI (2026-07-26; journal `2026-07-26-makine-kanali-denetim-izi`; karar: `docs/adr/ADR-008.md`):** borcun sorduğu "nereye BAĞLANACAK" sorusu **ayrı tablo** lehine ölçümle cevaplandı — `integration_events` (**migration v14**), `export_records`'a SIFIR dokunuş. Red olayları üretilmemiş dosyalardır (`filepath` NOT NULL → boş dize + uydurma `version`); `exports.ts` `MAX(version)`'ı kind FİLTRESİZ okuduğu için makine satırları **atölye sürüm sayacını ileri iterdi**; `SELECT *` yapan iki canlı rota (`exports.ts`, `present.ts`) izi **atölye UI'ına sızdırırdı**; `ON DELETE CASCADE` belge silinince **izi yok ederdi**; alanlar kolon olmadığı için sorgulanamazdı. **Stateless kararı BOZULMADI**, daraltıldı: `render.ts:8` gerekçesi *export_records'a yazmama* gerekçesidir, *iz tutmama* gerekçesi değil (9.2/675 denetim izini telemetriden ayırır); sürüm sayacı ve `_contract` klasör ayrımı aynen atölyenin. **ADR-005 tadil EDİLMEDİ** (`export_records.kind` sözlüğüne dokunulmadı — 8.5/641 üretim taksonomisi ile 7.4/524 denetim kaydı ayrı sınıflardır). Yazma **tek kapıdan** (`integration-audit.ts`, `brief_audit` emsali), append-only uygulama sözleşmesi **repo-taramalı testle** korunuyor; kayıt **imza kapısının arkasında** (503/401 yazmaz — imzasız istemci DB'ye satır yazdıramaz; 404/400/201 yazar); iki CHECK invaryantı yarım kaydı DB düzeyinde imkânsız kılar (üretildi ⇒ `sha256`+`filepath`; reddedildi ⇒ `error_code`); `ON DELETE SET NULL` + snapshot alanlar (`template_id`/`variant`/`target`) bağ koptuktan sonra da "ne istendi"yi cevaplar. Okuma ucu açıldı: `GET /api/documents/:id/integration-events`. **Artık karşılanan kanonik maddeler:** 7.4/524 (denetim kaydı) · 9.2/673 (entegrasyon hataları) · 9.1/665 (iz değişmezdir) · 9.1/666 + 5.6/410 (dış servise giden her veri kayıt altına alınır) · 9.5/699-701 (profil/bağlayıcı kullanım kontrol noktası) · GT-7/758 (denetim izi kapısı).
- [ ] **(3) `decoupe`/`png` contract enum'unda YOK (kayıtlı sınır):** Render Contract v1 `variant` enum'u yalnız `print|preview` taşır — yani kesim (`decoupe`, cam/vitrophanie) ve tekstil (`png`, `broderie`) çıktıları imzalı MAKİNE kanalından İSTENEMEZ; bu ürünler bugün yalnız atölye ucundan üretilir. Enum'u genişletmek MAJOR contract kararıdır (MULTI_REPO: iki tarafta EŞZAMANLI goal — B tarafı istemci + C tarafı uç, kanonik imza dizesi ve sürüm etkisi birlikte değerlendirilir). Ayrışmayı sessiz bırakmamak için nöbetçi kuruldu: `render-channel.test.ts` `DIS_KANALLAR` ile enum'un ES-KÜMELİĞİNİ çiviler.
- [ ] **(4) ÇAĞIRANA BİLDİRİM borcu — `profile_not_registered` bir DAVRANIŞ DARALTMASIDIR (hasım tur bulgusu):** paket bunu ilk yazımda "additive guard" diye nitelemişti; ölçüm düzeltti — dün 201 dönen bir istek (kimlik kaydında olmayan profil, ör. `kabul-fabrika`) bugün 400 alır. İstek şeması ve kanonik imza dizesi değişmediği için istemci KODU kırılmaz ve `RENDER_CONTRACT_V=1` korunur; daralan şey BAŞARI KÜMESİDİR (bilinçli güvenlik sıkılaştırması: ilan denetimsiz üretim kapatıldı). MULTI_REPO'nun "kırıcı değişiklik = MAJOR + iki tarafta eşzamanlı goal" kuralı açısından B tarafına (styva) BİLDİRİM borcu doğar — bugün fiilî etki yok (FLYER M7 çağıranı ilanlı `flyer` profilinden geçer), ama sözleşme ortağına haber verilmeden kapanmış sayılmaz.
- [ ] **(5) ÇAĞIRAN KİMLİĞİ alanı yok (denetim izi paketinin kayıtlı eksiği — ADR-008 "Kapsam dışı"):** `integration_events` hangi istemcinin çağırdığını TAŞIMAZ, çünkü bugün böyle bir bilgi YOK: `/render` tek **paylaşılan** secret ile doğrulanır (`RENDER_CONTRACT_SECRET`). Journal'ın "aktör ASLA null" dersi gereği sahte ya da hep-NULL bir aktör kolonu bilinçli olarak EKLENMEDİ — var olup boş duran alan, olmayan alandan daha yanıltıcıdır. **Tetik:** per-client secret / key-id (MULTI_REPO: B tarafıyla eşgüdüm) — geldiği gün additive `ALTER` ile kolon eklenir (v11/v13 emsali), migration gerektirir.
- [ ] **(6) Kriptografik HASH ZİNCİRİ v2 (değişmezliğin mekanik güvencesi):** v1'de append-only bir **uygulama katmanı sözleşmesidir** — yazma tek kapıdan (`integration-audit.ts`), UPDATE/DELETE yolu yazılmaz, repo taramalı test bunu çiviler (`brief_audit` emsali). Ama DB'ye doğrudan erişen biri satırı sessizce değiştirebilir; 9.1/665 "denetim izi değişmezdir" **prosedürel** olarak karşılanıyor, mekanik olarak değil. Journal'ın `prev`/`hash` zinciri emsal — bu tabloya taşınması ayrı pakettir (şema + geriye dönük satırların zincirlenmesi kararı).
- [ ] **(7) Denetim izi UI GÖRÜNÜMÜ yok:** okuma ucu açıldı (`GET /api/documents/:id/integration-events`) ama hiçbir ekran onu OKUMUYOR. `intake_records`'un "okunmayan iz" olması bu depoda KUSUR olarak ölçülmüştü — emsal değil, uyarı. EditorPage/ExportPanel'de belge bazlı makine-kanalı olay listesi (istek/red gerekçesi/sha256) ayrı pakettir; uç hazır olduğu için yalnız görünüm işi.
- [ ] **(8) TELEMETRİ sınıfı hâlâ karşılıksız (9.2/671-673, denetim izinden AYRI):** 9.2/675 iki sınıfı keskin ayırır — denetim izi hukuki/iş kaydıdır, değişmezdir, örneklenemez; telemetri operasyoneldir ve örneklenebilir. v14 tablosu YALNIZ birincisidir ve telemetri deposu olarak kullanılmamalıdır. Yapılandırılmış log · metrik (render kuyruğu ve süreleri, kalite kapısı sonuçları, tenant bazlı kullanım/kota, hata bütçesi) · dağıtık izleme bugün YOK → ayrı paket, ayrı depolama kararı.

### Composition Engine sonrası açılan borçlar (2026-07-19, B paketi)
- [x] **`menu-trifold` ve `menu-grid-cells` motora bağlandı — KAPANDI (2026-07-26, journal `2026-07-26-ce-baglama-trifold-grid`).** İkisi de `repeater.overflow` ilanını artık OKUYOR: trifold `composeColumns`'a tam bağlandı (eski `flowColumns`'un sabit kodladığı kategori yapıştırması `keepWithNext` callback'iyle birebir taşındı); grid-cells strateji sözlüğüne bağlandı (düşürme izni + ihlal raporu — ızgara MEKANİĞİ `layoutGrid`'de kalır: kategori şeritleri + multipage devam geometrisi `composeGrid`'in tekdüze hücre sözleşmesinde ifade edilemez, bilinçli/belgeli sınır). Çıktı DEĞİŞMEDİ ve bu, `origin/main@d229849`'dan çıkarılmış `__baseline.ts`'lerle diferansiyel testte kanıtlı (composition-differential.test.ts — şekil × format × kolon × akış kombinasyon taraması, birebir aynı). Ayrı görsel GT turu gerekmedi: altın kayıt yerine daha güçlü olan eski≡yeni diferansiyeli kuruldu.
- [x] **`generated/kabul-fabrika` motora bağlandı — KAPANDI (2026-07-26, ürün sahibi yetkisi; journal `2026-07-26-fabrika-motor-baglama`).** Ölçülen iki yara birlikte kapandı: (1) üretilen Template `items.slice` ile SESSİZCE kırpıyordu (yorumun iddia ettiği uyarı hiç üretilmiyordu) — kapasite/taşma artık `composeGrid`'de, strateji manifest ilanından, taşma görünür uyarı; (2) web `analyzeDoc` default dalı fabrika belgelerini menu-grid-cells analizine yönlendiriyordu (panel alakasız uyarılar gösteriyordu) — `TemplateEntry.warnings` köprüsüyle şablonun KENDİ analizi okunuyor. Emit değişti: prototipli her şablona `analyze.ts` üretilir. Eşdeğerlik: kapasite formülü birebir (referans-kopya testli, çivi 10), `shown` eski slice ile birebir; çizim konumları değişmedi (motorun esnetilmiş cellH'i çizime sokulmaz). Motor envanteri **5/5** — "ilan = davranış" repeater ailesinin tamamında.
- [x] **Tohumlu varyasyon (`designSeed` v1) — KAPANDI (2026-07-26, ürün sahibi paketi; journal `2026-07-26-designseed`).** `engine/seed.ts` deterministik çekirdeği (FNV-1a + mulberry32; `seededVariant` KAPALI kümeden seçer — "benzersizlik rastgelelik değildir") + menu-liste-premium'a `designSeed` number paramı (belgeyle taşınır, param panelinde kullanıcı kontrolü, 0 = taban). İlk varyasyon alanı: **boşluk ritmi** (4.4 listesinden) — kapalı 5'li çarpan kümesi, tüm satırlara aynı çarpan (simetri 4.3), taşma sözleşmesi/min-font dokunulmadı. Borcun şart koştuğu test yazıldı ve yeşil: **farklı tohum → FARKLI yerleşim** + aynı tohum → aynı + 0/yok → bugünkü birebir (diferansiyeller kanıt) + ürün kaybı sıfır. Motor request'ine alan bilinçli AÇILMADI (motor tüketmiyor — composition.ts NOT şerhi). **Follow-up (açık):** diğer şablon ailelerine ve diğer 4.4 varyasyon alanlarına yayma — her biri kendi görsel GT'siyle ayrı paket; tohumlu çıktının estetik onayı görsel turda ürün sahibinin.
- [x] **`balanceLastColumn` ilanı GERİ ÇEKİLDİ — KAPANDI (2026-07-26, ürün sahibi kararı; journal `2026-07-26-balance-geri-cekme`).** Borcun sunduğu iki seçenekten sahip geri çekmeyi seçti: alan + `balanceColumns` işlevi + yalnız onları ölçen 7 test kaldırıldı; yerine seed-emsali kaldırma notu (composition.ts NOT-2). Hiçbir üretim çağıranı geçirmiyordu → tüm canlı çağıranlar için davranış-dışı (kanıt: 39 diferansiyel dahil tam suite yeşil). Gerçek dengeleme gerekirse şablon bağlaması + insan GT ile geri gelir. Şerh: `metrics.imbalance_mm` ölçüm alanı DOKUNULMADI ama dış tüketicisi bugün yok — ayrı değerlendirme kalemi.
- [x] **`composeGrid.strategyViolation` okunuyor — KAPANDI (ölçüldü 2026-07-26):** `flyer/analyze.ts:120` ihlali `overflow-strategy-violation` uyarısına çeviriyor, `EditorPage.tsx:59` görüntülüyor (B paketi sonrasında kapanmış, borç kaydı güncellenmemişti). CE-bağlama paketiyle `menu-grid-cells` de aynı sözleşmeyi uyguluyor (`gridOverflowWarning`, ölü-yol dalı birim-testli).
- [x] **GT-2 görsel turu KOŞULDU ve İMZALANDI (2026-07-26; journal `2026-07-26-gt2-tur-hazirligi` seq 28, insan aktör `urun-sahibi`).** Altyapı: `docs/gt2-tur/` — tek komutla üretilen deterministik 20/50/100/200 galerisi (98 kart) + protokol; ölçülebilir alt-kriterler makinede (`engine/gt2-invariants.test.ts`, 25 test). Yargı kalemleri (sırıtmayan reflow, simetri, adaptif tipografi) ürün sahibince galeri üzerinde gözle doğrulandı; kanıt kapı kaydında. Tur, gelecekteki layout paketlerinde aynı galeriyle tekrarlanabilir (üretici deterministik).

## Pilot dönem (aktif — Faz 4 kapanışıyla)
- [ ] **Pilot iş dönemi başladı — FAZ5 paketi pilot saha notlarıyla şekillenecek; bu dönemde gelen her yeni fikir/talep M10 gereği TODO'ya yazılır, sırasız uygulanmaz.**

### Pilot saha kayıtları (UYGULAMA YOK — kayıt biçimi: `- [YYYY-AA-GG] tek satırlık not / fikir / sürtünme`)
- [2026-07-08] 60+ ürünlük el yazısı menü tek tek girilemezdi — katalog toplu içe aktarma (yapıştır-parse) FAZ5 adayı; pilotta mimar+CC eliyle yüklendi. → **FAZ5 §4'te ele alınıyor.**
- [2026-07-08] Sunucu başlatma sürtünmesi 4. kez yaşandı; kök neden netleşti: tsx watch zinciri (apps/server) bazen 3001'e hiç bağlanmadan askıda kalıyor (EADDRINUSE değil — port boş ama süreç canlı/hayalet), vite tarafı ayakta kalınca proxy sürekli ECONNREFUSED basıyor. start-tezgah.bat artık kendi kendini kontrol ediyor: önce iki portu da yoklar, zaten sağlıklıysa dokunmaz; değilse proje yoluna göre (PowerShell CommandLine filtresi) hayalet node.exe süreçlerini temizleyip yeniden dener, sonucu pencerede "OK"/"BAŞARISIZ + öneri" olarak açıkça yazar (mimar onaylı, script-only). Kalıcı çözüm (tepsi uygulaması / otomatik başlatma / gerçek process-supervisor) Faz 5/S adayı.
- [2026-07-08] 3 sütunlu A4 flyer: kullanıcının referans tasarımının vektör kaynağı YOK (yalnız JPG). Karar: FAZ5'te (a) yolu seçildi — `menu-liste-premium`'a A4 3-sütun yoğun varyant eklenir (mimar #14). → **FAZ5 §3'te ele alınıyor** (yalnız a4-portrait; a4-landscape bu şablonda yok, pilotta ihtiyaç doğarsa yeniden değerlendirilir).
- [2026-07-08] Kullanıcı font yükleme talebi (FAZ5 adayı): arayüzden kendi font dosyasını (woff2/ttf) ekleyip başlık/gövde rollerinde seçebilme; glif kapsamı (TR+FR) otomatik kontrol edilmeli. Not: rol-bazlı font sistemi (başlık/gövde tek yerden) zaten mevcut — kullanıcıya gösterildi. → **FAZ5 §7'de ele alınıyor** (bekçi kümesi mimar #18).
- [2026-07-08] İLK GERÇEK İŞ TAMAMLANDI: Aras Restaurant, 75 ürün / 10 kategori, menu-liste-premium A4 2 sütun, v1 preview+print (crop marks doğru, aksanlar TR+FR temiz, 2 sayfa). Marka kiti düzeltmesi sonrası v2 alınacak. Pilot akışta göze çarpan: şablon/format/sütun keşfi kullanıcıya başta karışık geldi (Resimli Izgara'ya yanlışlıkla geçildi) — FAZ5 notu: belge açarken "bu iş için hangi şablon?" yönlendirmesi düşünülebilir. → **FAZ5 §8 (şablon seçim rehberi)'nde ele alınıyor.**
- [2026-07-09] FAZ 5 main'de: 3 sütunlu yoğun menü, katalog yapıştır-içe aktarma, tıkla-takas, sürükle-bırak, font yükleme, şablon rehberi, QR dijital menü v1 kullanımda. Pilot devam ediyor.
- [2026-07-09] İkinci gerçek müşteri: Arriva (CHF, çok-boyutlu pizza fiyatı tek-fiyat modeline not olarak sıkıştırıldı — FAZ 6 adayı: çoklu fiyat sütunu desteği).
- [2026-07-09] FAZ 6 main'de: fabrika SVG bekçileri (ölçü türet-önce-sor, outline/font/varlık tespiti), künye (manifest-provenance), serbest ölçü + custom bleed/crop, sıfır-slot dekor hattı, InDesign→SVG rehberi (docs/arsiv-ithalati.md). Migrationsız faz. Sıradaki pilot işi: kullanıcının arşivinden ilk gerçek InDesign→SVG dönüşümü.
- [2026-07-10] **FAZ 7 AÇILDI — öncelik Sipariş Modu** (mimar+kullanıcı kararı). Atölye içi + saha (tablet/telefon, mobil-öncelikli) tıkla-seç sipariş alma aracı — restoran POS'u DEĞİL. Vizyon: sektör profilleri (hazır kategori+ürün setleri) + ürün-başına akıllı sorular + içerik çipleri (klavyesiz) + çok-dilli (TR tıkla / FR-DE bas) öğrenen malzeme kütüphanesi + kapanış doneler/şartlar çeklisti. Arriva/Aras müşteri-dönüş hattı PARKTA (dönüş gelirse ayrı paket). Keşif (salt-okuma) bu oturumda yapıldı: PAKET-F7-KEŞİF envanter raporu + veri-modeli iskeleti mimara döndü.
- [2026-07-12] FAZ 8 keşfi başladı (yüzeyler+mockup); resmi faz açılışı kullanıcı QA'sı temiz çıkınca. PAKET-F8-KEŞİF envanter raporu (salt-okuma) mimara döndü.
- [2026-07-12] **PAKET-F8-A teslim (main'e merge `4127d86` — "bekliyor" kaydı bayattı, defter düzeltmesi journal `2026-07-28-taslak-yarasi-f7-defter`):** yüzey köprüsü — intake çeklistinde yapısal `surfaces[]` (kind: vitrine/tabela/garment/diger · label · w/h cm · not) → commit'te müşteri-düzeyi `client_surfaces` tablosuna (migration **v10**) UPSERT ((client_id, foldTr(label)) — "bir kez gir, hep kullan", çift kayıt yok; tarihçe = intake_records, ayrı history YOK). Serbest-metin ölçü/yüzey notları KALIR (M8). **Atomiklik:** bozuk surfaces → transaction'dan ÖNCE 400 (fail-loud, sessiz yarım kayıt yok — izole smoke 11/11 kanıtladı). ClientDetailPage "Yüzeyler" paneli (liste + sil, D5). Vitro/enseigne belge ölçü ön-dolumu (D6; garment yok). Taslak v2→v3 GERÇEK additive migrate. **362 yeşil**, kod-only geriye-uyumlu. **Devam adayları:** (a) order_items otomasyonu (intake yüzey → sipariş kalemi — ERTELENDİ, sipariş-defteri iş akışı kararları gerekir); (b) garment yüzey ön-dolumu (alan-preset'li, ayrı); (c) SurfaceKind→SceneKind eşlemesi + yüzey→mockup sahnesi köprüsü (F8-D); (d) yüzey ölçüsü sınır-dışı (>2000cm) commit'i 400'lüyor — istenirse çeklist adımında istemci-tarafı uyarı (bugün fail-loud sunucuda).

- [2026-07-12] **PAKET-TUR-FIX teslim (main'e merge `4f84980` — "bekliyor" kaydı bayattı, defter düzeltmesi journal `2026-07-28-taslak-yarasi-f7-defter`):** görsel tur kırıkları — **FIX-1:** yüzey silme DELETE bug'ı (kök: `api.ts` http helper'ı gövdesiz isteklere de `Content-Type: application/json` koyuyordu → Fastify boş gövdeli DELETE'i 400'lüyordu; fix: başlık yalnız gövde varsa — TÜM gövdesiz DELETE'ler düzeldi; ölçülü smoke: (a) json-başlıklı DELETE→400 "Body cannot be empty" ÜRETİLDİ, (b) başlıksız DELETE→200+satır gitti; x'in gerçek 2 yüzeyi el değmedi). **FIX-2:** A4 chrome başlığı dil-duyarlı (`resolveChromeTitle`: tr=MENÜ · fr=NOTRE CARTE · de=SPEISEKARTE ß'siz; override kazanır; liste-premium+grid-cells ikisi de). **AÇIK sonucu (kod bug'ı DEĞİL):** "Escalope" seed'de TR adı da "Escalope" (F7-B2 loan-word kararı — `pickML(tr)` doğru basıyor; istenirse "Eskalop" bir TOHUM İÇERİK tercihi, ayrı iş); "frites" görülen kagıthane/z **fr müşterileri** (doğru dil); bonus: x'in Türkçe kategorileri HF3'ün saha kanıtı. 366 yeşil.
- [ ] **Tohum içerik tercihi (TUR-FIX AÇIK'tan):** seed loan-word adları ("Escalope" tr=fr aynı) operatörce yadırganırsa TR karşılıkları ("Eskalop"?) tohum güncellemesi olarak ele alınır — kod işi değil, içerik kararı; kullanıcı geri bildirimi bekler.
- [2026-07-14] **PAKET-PART-B teslim (dal `paket-part-b`, T3; merge kutusu yönetişimden):** C'nin İLK program-doğumlu goal'ü — **adım-0:** /docs governance bootstrap (PROGRAM_COCKPIT v14 pointer-şerhli + GOAL_QUEUE/ROADMAP + adr/ADR-001..006 + EXECUTION_DIRECTIVE işaretçi + MULTI_REPO v1.2 kopyası + kök CHANGELOG/VERSION/RELEASE_NOTES + README boot satırı; repo gerçeği şerhi: docs/ teknik rehberle zaten vardı, governance dosyaları yoktu; 00_READ_FIRST metni teslim edilmedi → additive bekler). **adım-1:** Render Contract v1 engine şimi (`POST /render` HMAC'li, env'siz 503 kapalı-kapı, stateless, `_contract/` çıktı; watermark v1'de uygulanmaz — meta'da görünür; 8 uyum testi + canlı smoke 8/8: 401/401/201+sha256). Kısıt kanıtı: PrintPage/exports/MockupPage diff BOŞ. 392 yeşil.
- [2026-07-14] **PAKET-F8D teslim (dal `paket-f8d`, merge kutusu + T3 PART-B paketi Fable'dan birlikte):** mockup profesyonelleştirme — **H1 damga:** "MOCKUP — baskı provası değildir / ne pas utiliser pour l'impression / nicht zum Drucken verwenden (ß'siz)" YALNIZ MockupPage'de KOŞULSUZ (dil-duyarlı Δ2: `mockupWatermarkText(client.menu_language)`); JPG'ye piksel gömülü; grep-ispatı MockupPage=VAR, PrintPage/exports=0. **H2 anti-kaçış:** `MOCKUP_MAX_W=1600` shared'da (MockupPage+server mockup rotası dedupe; yüksek-çöz yolu bilerek YOK — istenirse F8-E kapısı). **H3 premium sahne altyapısı:** SceneSettings ADDITIVE shadow/overlay (migrationsız; eski sahne default'la) + `SCENE_STYLE_PRESETS` iskeletleri (soft_shadow/vitrine_glare); **ScenesPanel preset UI wiring AYRI tur (Δ1)**. **H4 kurumsal çok-yüzey presentation → F8-E'ye ertelendi.** Δ3 gerçek-sahne canlı smoke 8/8 (Aras Grill sahnesi: 201+kind=mockup · genişlik 1400≤1600 · damga bandı lum 48 vs 86 · net-zero temizlik · 3 sahne el değmedi); eski-server/yeni-web dayanıklılığı MockupPage'de (smoke eski server'la geçti — canlı kanıt). 383 yeşil.
- [x] **F8-E adayları (F8-D'den):** (a) çok-yüzey presentation + (b) yüksek-çöz kapısı → **PAKET-F8E'de UYGULANDI** (aşağıdaki teslim kaydı); (c) ScenesPanel preset-uygula UI → **SCENES-UI-01** (ayrı paket, kendi görsel turunu bekler).
- [2026-07-17] **PAKET-F8E teslim (dal `paket-f8e`, merge kutusu Gate4 sonrası yönetişimden):** F8-E — **H4 çok-yüzey sunum:** `POST /api/projects/:id/present` additive `mockup_mode:"per_scene_kind"` (default `"last"` = eski davranış BİREBİR; smoke'ta DOM+snapshot kanıtlı) → belge×sahne-türü EN SON mockup ayrı tam sayfa + tür etiketi (Vitrin/Cephe/Giyim/Genel) + `client_surfaces` yüzey etiketi/cm satırı (`surfaceToSceneKind` — schemas.ts F8-A eşleme borcu KAPANDI) + kapak "N tasarım · M yüzey" + BAT Surfaces satırı; seçim SUNUCUDA (DTO snapshot taşımaz), plan `mplan` URL paramıyla /present'e. **Kapılı hires (ADR-005 tadili — şerh dosyada):** `POST /api/documents/:id/mockup-hires` — re-onay literal'i ("baskı için değildir") şemada `z.literal` (yanlış/eksik → 400) · tavan `MOCKUP_HIRES_MAX_W=3200` · kind `mockup_hires` (sözlük: docs/export-kinds.md) · damga KOŞULSUZ; MockupPage'de TEK-diyalog "Yüksek çözünürlük (ekran)" eylemi (`shot=1`'de render edilmez → JPG'ye sızamaz; ayrıca görüntü alanının altında). Varsayılan uç/tavan AYNEN — 2400px fotoda ÖLÇÜLÜ çift kanıt: default 1600 · hires 2400≤3200. **Canlı smoke 31/31** (tamamen geçici asset/sahne/belge/yüzeyle; gerçek veriye dokunulmadı; net-zero; app.db 417792 boyut-değişmez D-27). **412 yeşil** (server 64 · shared 209 · templates 139), typecheck 0, migration YOK. Watermark gerçek uygulaması → CONTRACT-WM-01 (tetik: C1 ilk tüketim tarifi).
- [2026-07-18] **PAKET-LY1 teslim (dal `paket-ly1`; PA-9; merge kutusu GT sonucuyla dönecek — /atolye işaretli yüzey):** **P3 CAP-LAYER-01** — LY1: katmanlı model (`CanvasLayer` + `CanvasDocSchema`; CD v1'e `canvas?` ADDITIVE-OPSİYONEL alan — **D-35(c) kapısı açıldı, cd_version 1 KALDI**; yokluk=eski davranış) + reducer katman aksiyonları (add/activate/remove[son-katman-silinemez]/reorder/lock/visible — tek-kapı) + kilit/görünürlük korkulukları (kilitli-gizli aktif katmanda sahne mutasyonları NO-OP + m.13a dersi: GÖRÜNÜR amber şerit). LY2: kalıcılık köprüsü — `/atolye?doc=<id>` bağlama + Kaydet=MEVCUT PUT (restore canvas'a dokunmaz→korunur, testli). **⚠ KALICILIK BULGUSU (DUR-raporlu):** PUT `canvas`'ı parse ediyor AMA `documents` tablosu kolon-temelli (canvas kolonu YOK) → alan HİÇBİR yere yazılmıyor; kalıcılık smoke'u bilerek KIRMIZI (22/24'ün 2 kırmızısı). Migration bu pakette YASAK → depolama kararı yönetişimde (öneri: v11 `canvas_json` mini-kolon). Kalıcılık DIŞI her şey canlı-yeşil. LY3: katman paneli (klavyesiz tam; aktif vurgusu/👁/🔒/↑↓/✕) + **D-46: katman-silme FIX-1 deseni (onaysız + 5sn Geri-al=undo)**. LY4: görünür ↺ Geri al / ↻ Yinele + Ctrl+Z/Y (past/future, derinlik 50; seçim/aktivasyon geçmişe girmez). 23 eski canvas testi katmanlı modele taşındı (iddialar aynen) + yeni testler. Şerh: LY3+LY4 tek sayfa yeniden-yazımı olduğundan tek commit. (dal `paket-hf1`; PA-8 İLK ZİNCİRLİ; merge kutusu PA-9'la dönecek):** üç GT bulgusunun hotfix'i — **FIX-1 (m.1, çip:a — ürün sahibi):** onay-koşulu AYNEN; onaysız Kaldır artık 5sn **Geri al** tostu kazanır (shared captureRemoval/restoreRemoval saf çekirdek + intakeStore lastRemoved [partialize DIŞI — taslağa yazılmaz] + ≥44px tost düğmesi; ürün ESKİ sırasına döner). **FIX-2 (m.4):** kök = gaps yalnız ÇİP çözümünü izliyordu, pickML ad-fallback'i sessizdi → shared `intakeNameGaps` (pickML zinciriyle aynı sıra; kategori dedup'lu) özet'te çip-gap'leriyle BİRLEŞİK basılır; **OTOMATİK REPRO-TESTİ** kutu şartıyla: fr müşteriye TR-only özel ürün → gap GÖRÜNÜR. **FIX-3 (m.10):** EditorPage mockup modalında sahne başına görünür **"Canlı önizleme ↗"** bağlantısı (/mockup/:id?scene=) — hires eyleminin kapısı açıldı; KK-7a GT kalemi: "linke tıkla → canlı önizleme + 'Yüksek çözünürlük (ekran)' düğmesi görünüyor mu". Kapsam-dışı teyit: canvas/AtolyePage · Print/Mockup/Present · şablonlar · CD şeması · export uçları · lockfile DOKUNULMADI. **FIX-A (m.13a):** add-mode'da mevcut şeklin ÜSTÜNE tıklama artık EKLEME değil SEÇİM — hem reducer kemerinde (`shapeAtPoint` bbox hit-testi, üstteki kazanır, ham-nokta ile; çoğalma reducer'da ölür) hem UI'da (seçim tek-kaynak stage-mousedown; per-shape handler kalktı); yeni şekil YALNIZ boş alana tıklayınca doğar. **FIX-B (m.13b):** araç çubuğuna görünür **Sil** düğmesi (seçim yokken pasif; reducer remove'dan) + Del tuşu yolu seçim düzelince çalışır durumda. +4 test (shapeAtPoint · üstteki-kazanır · sayı-sabit · ham-nokta); FIX-A'nın yeni kuralı 2 eski CV1-3 fixture'ında nokta-çakışması düzeltmesi istedi (iddialar aynen — şerhli). **SAYAÇ ANOMALİSİ AÇIKLANDI (+786→+885):** yetkisiz yazma DEĞİL — kullanıcı tur sırasında fabrikadan "pizza" şablonu üretti (sunucu logu: POST /api/factory/generate @23:13:49, 127.0.0.1); fabrika tasarımı gereği kaynak koda yazar (mimar #12): pizza/ 97 satır + generated/index.ts +2 = +99 birebir. **pizza dosyaları commit'lenmemiş KULLANICI İŞİ olarak ağaçta bırakıldı** (bu commit'e alınmadı; kararı ürün sahibinin). Canlı FIX-smoke 4/4 (aynı-noktaya-3-tık→1 şekil · Sil aktif+çalışıyor · Del 1→0). **449 yeşil** (server 67 · shared 243 · templates 139), typecheck 0. **P2 Konva iskeleti** — CV1-1 deps: `konva 9.3.22` + `react-konva 18.2.16` (İLK yeni bağımlılık; MIT; lockfile kanıtı: silinen version/resolved satırı 0, yalnız yeni bloklar + 3 devOptional bayrağı). CV1-2: izole `/atolye` (React.lazy, chrome'suz grup; Layout/mevcut sayfalara sıfır dokunuş) + Stage/Layer + pan/zoom + dev-only canvasStore (kalıcılık YOK — D-35(c)). CV1-3: izin-listeli araçlar (Seç/Kare/Elips/Metin) + seç/taşı (canlı dragBound sahne kilidi) + kısıtlı Transformer (min-boyut, döndürme kapalı) + Del + çift-tık metin; korkuluk SAF çekirdeği `shared/canvas.ts` (clampToBounds · snapToGrid · zoomAt · canvasReduce tek kapı; +19 test — beklenti ~10-12 üstü, şerhli). CV1-4: ADR-002 federasyon şerhi AYNEN dosyada + kanıt seti. **BÜTÇE: canvas chunk 91.83KB gzip (≤160 hedef ✓✓); ana bundle 180.34→180.91 gzip (Δ +0.57KB — yalnız lazy-rota kaydı, konva DEĞİL; şerhli).** SMOKE 10/10: chunk izolasyonu ağ kanıtı (kök+/siparis konva istemiyor) · /atolye render · /print baseline birebir ({216,303,1}) · gerçek export 201+net-zero · app.db 417792 aynen. **445 yeşil** (server 67 · shared 239 · templates 139), typecheck 0. **GT beyanı: ERTELENDİ (2. kez) → ilan gereği P2 MERGE GT-KAPILI.** Kapsam-dışı teyit: katman/kaydetme/CD-bağı/şablon/undo/export-bağı/vitest-web/EditorPage/migration/styva YOK.
- [2026-07-18] **PAKET-LY2b teslim (dal `paket-ly1`; D-48 — programın İLK ONAYLI MIGRATION'ı, Otonomi m.4 icrası):** v11 `documents.canvas_json TEXT` (additive TEK kolon; eski satır NULL=yokluk; veri dönüşümü YOK) + `rowToDocument`/POST/PUT kalıcılık bağı (~6 satır; restore UPDATE'i kolona bilinçli dokunmaz → snapshot geri-yükleme canvas'ı DB düzeyinde de KORUR — LY1 iddiasının sunucu yarısı). LY1'in 2 kırmızısı canlıda GERÇEKTEN yeşil: PUT canvas→200 · taze GET'te canvas dolu-birebir (kaydet→yenile→geri-geldi API düzeyi; tarayıcı düzeyi = K3 GT m.L4) → **smoke 24/24**. Çapraz kanıt: canvas'sız partial PUT canvas'ı KORUR · eski belge bayt-birebir · net-zero (21 belge/21 NULL aynen; smoke belgesi silindi). Şema teyidi: `user_version=11` · kolon `PRAGMA table_info`'da · 21/21 eski satır NULL · eski belge default'la açılır (GET'te canvas alanı yok). **app.db YENİ BASELINE: 430080 B · 2026-07-18 11:47** (D-48 migration-baseline kuralı — değişim BEKLENEN; D-27 şerhi silme içindi; önceki 417792/14.07). +4 migration replay testi → **487 yeşil** (server 71 · shared 277 · templates 139), typecheck 4/4.
- [2026-07-18] **F1 PİLOT P5 teslim (garment intake + beden×adet; migrationsız — v13 yetti; GT-F1/P5 İNSAN TURU BEKLİYOR):** **İŞ-0 tur-sonu temizlik (ürün sahibi `gt-p4-brief: sil`):** GT-F1/P4'ün bıraktığı 1 menü brief'i (18 audit satırı · 2 dosya · 2 brief-asset'i) DB-düzeyi tek seferlik temizlikle silindi → briefs/audit/files **0/0/0**, assets 13→11 (P3 seviyesine döndü), diskten 4 dosya kalktı, `user_version=13` sabit. **P5:** garment ailesi menüyle AYNI yaşam döngüsünü kullanır (yeni altyapı YOK) — fark yalnız alan matrisi. **Beden×adet matrisi (sıfırdan):** `spec_values.size_distribution` (v13 kolonu; yeni migration GEREKMEDİ), toplam adet **HESAPLANIR** (`f1TotalQuantity`), **0/negatif adetler DÜŞER**, boş dağılım rozette eksik görünür; beden seti v1'de sabit XS-XXL (tenant beden modeli repo'da yok — 14d tek-tenant; gerekirse ayrı karar). **spec_values YAZMA BEKÇİSİ (yeni):** yalnız SpecRef'te tanımlı ve kolon/türetme kaynaklı OLMAYAN anahtarlar yazılabilir → uydurma alan · yabancı-aile alanı (`qr_target_url` garment'a) · çift-kaynak alanı (`delivery_deadline`) **400 unknown_spec_keys**. **Adlandırma hizalaması:** P2'deki `size_quantity` → **`size_distribution`** (kanonik; spec_values anahtarıyla birebir). **UI:** `MenuBriefPage` → `BriefPage` (iki aile tek sayfa; aile seçimi brief açılışında): ürün tipi · baskı yeri (GARMENT_AREAS, ürün tipine göre filtreli) · renk · teknik (**DTF → "alfa-PNG teslim edilir" bilgi notu** rozette) · beden×adet matrisi + **canlı toplam** · tasarım dosyası (rol `tasarim`) · koşullu baskı boyutu. Kapı: typecheck 4/4 · lint 0 · **606 test** (server 124 · shared 343 · templates 139; +6) · build PASS · ana bundle 187.45→**188.46 gzip (+1.01KB)** · canvas chunk 93.29 (≤160 ✓) · **user_version=13 SABİT** · **intakeStore/IntakeProductsStep/routes/intake dokunuşu 0** · canlı restart: sectors 5 · ingredients 61 · clients 6 · briefs-audit-files 0/0/0 · app.db 516096/20:46 (temizlik sonrası, değişmedi).
- [2026-07-18] **F1 PİLOT P4 teslim (menü intake + brief yaşam döngüsü; GT-F1/P4 İNSAN TURU BEKLİYOR):** **v13** `briefs.spec_values_json TEXT NOT NULL DEFAULT '{}'` (ürün sahibi onayı `spec-values: 1` — programın 3. onaylı migration'ı; BLOCKER-3'ün çözümü: format/orientation/qr_target_url/print_quantity/print_material/color_font_choice'un evi. SWISS sözleşme kolonlarına GÖMÜLMEDİ — D-48'in "params'a gömme" reddi emsal alındı). **Uçlar:** `POST /api/briefs` (DRAFT + audit; idempotency_key sunucu üretir; **F1.6: aynı anahtar → mevcut brief döner, ikinci satır doğmaz**) · `GET` (brief + completeness + **isimli eksikler** + next_states) · `PATCH` (spec_values BİRLEŞTİRİLİR; **durum YAZILAMAZ**) · `POST /transition` (**TEK KAPI**: canTransitionF1; DRAFT→INCOMPLETE→READY_FOR_DESIGN→DESIGN_IN_PROGRESS canlı, gerileme kayıtlı; **REVIEW/READY 501 not_yet_available** — P7). **F1.7 BAĞLANDI** (P3'ün açık ucu): dosya invalidate → açık REDDET doğarsa iş guard'dan geçerek INCOMPLETE'e düşer + state_changed audit. **brief-view.ts:** değer kaynakları TEK yerde; **fiyat eksiksizliği katalogdan OKUNUR** (14e — brief'e kopyalanmaz). **Web:** `lib/specJoin.ts` SpecRef↔manifest TEK modül (dağınık birleştirme yok) + **KAPASİTE UYARISI** (`kapasite-uyarisi: p4` kararı; P4/D ölçümü: 200 üründe grid 180, trifold 126 ürün düşürüyor → format seçiminde "N ürün SIĞMIYOR" görünür) · yeni `/brief` sayfası: kalıcı **eksik-bilgi rozeti (F1.2)**, format/dil/çıktı seçimi, teslim tarihi, koşullu alanlar (QR-URL / adet+malzeme / renk-font), logo yükleme (P3 ucu) + uyarı **kayıtlı onay** akışı, dosya geçersiz işaretleme, durum düğmeleri. **ŞERH — kutu sapması:** menü yolu `/siparis` bileşenine GÖMÜLMEDİ, ayrı `/brief` rotası + `/siparis` 1. adımda görünür giriş linki oldu; gerekçe: brief AYRI yaşam döngüsü (durum+audit+dosya politikası) ve GT-işaretli `IntakeProductsStep`'e **SIFIR dokunuş** hedefi. Sonuç: `intakeStore` SCHEMA_VERSION **bump edilmedi** → kullanıcı taslakları ATILMIYOR (kutudaki bump kalemi gereksiz kaldı). **Ayrıca:** ack'ler görünür ama tamlığı DEĞİŞTİRMEZ (alan-waiver kodu açılmadı — dürüst sınır, gerekirse ayrı karar). Kapı: typecheck 4/4 · lint 0 · **600 test** (server 118 · shared 343 · templates 139; +18) · build PASS · **ana bundle 183.22→187.45 gzip (+4.23KB, ilk UI paketi)** · canvas chunk 93.29 (≤160 ✓) · `user_version=13` · **intake dosyalarına dokunuş 0** (git kanıtı) · canlı restart sonrası sectors 5 / ingredients 61 / clients 6 aynen, briefs-audit-files **0/0/0**. **app.db YENİ BASELINE: 516096 B · 2026-07-18 19:28** (önceki 471040/17:22).
- [2026-07-18] **F1 PİLOT P3 teslim (file policy + audit bağı; migrationsız, dependency EKLENMEDİ):** **A/shared `file-policy.ts`** — saf sınıflandırma; kod sözlüğü `brief_audit.warning_code` ile birebir: REDDET(4: bozuk_dosya·desteklenmeyen_tur·zorunlu_dosya_eksik·olcu_belirlenemedi) · UYAR+onay(3: dusuk_dpi·guvenli_alan_riski·font_riski) · BİLGİ(4: cmyk/icc/pdfx_dogrulanamadi + pdf_derin_dogrulama_yok). **Keşif G düzeltmesi uygulandı:** renk profili doğrulanamıyorsa SESSİZ kalmıyor, üç not açıkça üretiliyor. DPI eşikleri (250/150) `checkDpi` ile birebir ama **shared'da YENİDEN BEYAN** (shared→templates importu yok; testle sabitlendi — drift riski kayıtlı). **B1 genel uç sertleştirmesi:** bozuk dosya 500→**400 policy-red**; tür listesi DEĞİŞMEDİ (webp korunur); türevler ÖNCE hesaplanıp dosyalar SONRA yazılıyor → yarım işlemde artık dosya kalmıyor. **B2 brief sınırı** (`routes/brief-files.ts` + app.ts kaydı): upload (spec v1 türleri; **webp burada RED** — genel uçla bilinçli fark) · PDF yalnız-sakla (%PDF imzası + boyut; derin doğrulama v1'de YOK → BİLGİ notu) · raster/svg sharp parse · aynı rolde **version++** · her karar audit satırı · `warnings/:code/ack` **audit beşlisi TAM** (acknowledged_by boş olamaz — tek-tenant 14d, kullanıcı modeli yok: dürüst sınır) · **F1.5 rota düzeyinde: REDDET koduna istisna verilemez** (BİLGİ notu da onay istemez) · `files/:fileId/invalidate` status+audit (**durum gerilemesi ÇAĞRILMAZ** — tüketici paketin işi). brief_audit'e YALNIZ INSERT → append-only tarayıcı testi yeşil. **KUTU DIŞI İKİ EK (şerhli):** (1) `paths.ts` `TEZGAH_DATA_DIR` seam'i — P0 deseninin devamı, yükleme testleri repo `data/`'yı kirletmesin (env yoksa üretim yolu birebir); (2) `assets.ts` ortak-havuz filtresi `id NOT IN (SELECT asset_id FROM brief_files)` — müşterisiz brief'in logosu başka müşterinin seçicisine düşmesin (brief_files boşken sonuç birebir eski). Kapı: typecheck 4/4 · lint 0 · **582 test** (server 100 · shared 343 · templates 139; +31) · build PASS · **web bundle hash yine değişmedi** (`index-BZ2wn0Ij.js`) · `user_version=12` sabit. **CANLI NET-ZERO (restart sonrası):** bozuk dosya → 400 policy-red; assets 11→11 · orig dosya 11→11 · briefs/brief_audit/brief_files **0/0/0** · app.db 471040/17:22 **birebir**.
- [2026-07-18] **F1 PİLOT P2 teslim (Spec-referans + completeness engine; migrationsız, saf shared):** **§7 HİZALAMA — ÜÇ SAPMA DÜZELTİLDİ** (P1'in şerhi kapandı): (1) `INCOMPLETE→DESIGN_IN_PROGRESS` kısayolu KALDIRILDI — K4 "eksik brief'le tasarım başlar" bir KENAR değil, `INCOMPLETE→READY_FOR_DESIGN`'ın design_readiness guard'ıdır; (2) `DRAFT→READY_FOR_DESIGN` kısayolu KALDIRILDI (zincir doğrusal); (3) `%100` guard'ı PRODUCTION_READY'den **READY_FOR_PRODUCTION_REVIEW girişine** taşındı + **"açık REDDET yok"** (openRejects) ve **"insan onayı"** (approvedBy) şartları eklendi. **f1-spec.ts:** DB tablosu YOK — `template_id+format` manifest REFERANSI (geometri kopyalanmaz; shared→templates importu YOK, döngü olurdu); iki aile matrisi VERİ olarak (menü: A4P/A4L-trifold/A3P-grid **DL YOK** · dil≥1 · içerik iskeleti · publications | fiyatlar · logo dosyası · teslim + koşullu QR-URL/adet/malzeme/renk-font · yön koşulu serbest formatta — garment: ürün tipi · yer≥1 | renk · beden×adet · teknik · tasarım dosyası · teslim + koşullu boyut/konum); **KEŞİF DÜZELTMESİ uygulandı:** garment tekniği `broderie|genel_baski`, `decoupe` YOK (vitro ailesine özgü), **DTF reddedilmez → BİLGİLENDİRİR** ("alfa-PNG teslim edilir"). **f1-completeness.ts:** saf motor; payda = production_pre zorunlu + AKTİF koşullu + dosya şartları (opsiyoneller HİÇ girmez); kayıtlı istisna kalemi karşılar ve çıktıda TAŞINIR (Pack listesine); **F1.5: REDDET-sınıfı istisnayla KARŞILANAMAZ** + §4 gereği design önkoşulu da waive EDİLEMEZ; invalid dosya karşılanmış sayılmaz (F1.7 zemini); yüzde AŞAĞI yuvarlanır (99.6 asla 100 okunmaz); eksikler İSİMLİ döner (F1.1). Kapı: typecheck 4/4 · lint 0 · **551 test** (server 85 · shared 327 · templates 139; +32) · build PASS · **web bundle hash P1'le BİREBİR AYNI** (`index-BZ2wn0Ij.js`, 183.22 gzip — P2 bundle'a 0 bayt ekledi) · **rota dokunulmazlık: `apps/server/src/routes` diff eşleşmesi 0**.
- [2026-07-18] **F1 PİLOT P0+P1 teslim (doğrudan main, paket başına TEK commit; D-59/60/61):** **P0** route-test harness — `buildApp()` app.ts'te (dinlemeden döner; kayıt sırası/prefix/health gövdesi/hata işleyicisi BİREBİR), index.ts yalnız migrate+listen, `db.ts` enjekte edilebilir (`openDatabase` + `TEZGAH_DB_PATH` import-anı yolu + `setDatabase` takas seami + `migrate(target?)`); **per-rota DI bilinçli YAPILMADI** (24 dosya = davranış riski; enjeksiyon bağlantı katmanında, ESM canlı bağ sayesinde rota imzaları değişmedi). Kanıt: app.test.ts (health · İZOLE DB'de clients=[] · 404 yolu) + canlı restart (health 200, 6 müşteri, app.db bayt-birebir). **P1** F1 Brief domain — **v12** (D-61 onaylı, programın 2. onaylı migration'ı): `briefs` 17 kolon (13 sözleşme alanı + status CHECK 6 durum + damgalar; **idempotency_key UNIQUE = F1.6'nın DB garantisi**; **customer_ref ON DELETE SET NULL — ŞERHLİ SAPMA**: repo'nun CASCADE emsalinden bilinçli ayrılma, gerekçe brief_audit'in değişmez-tarihçe olması, yönetişim tasdikine) · `brief_audit` 10 kolon (warning_code/acknowledged_by/acknowledged_at/reason/source_file_version) · `brief_files` 8 kolon (version+valid/invalid, F1.7 zemini; **assets'e DOKUNULMADI**) + 4 indeks. Shared `f1-state.ts`: iki eşik AYRI (design_readiness ≠ production_completeness), "eksik brief ile tasarım BAŞLAYABİLİR" testli, gerileme yalnız reason+recordedBy ile, üretim kapısı yalnız PRODUCTION_READY (F1.1 çekirdeği). **APPEND-ONLY sözleşmesi repo-taramalı testle korunuyor** (brief_audit'e UPDATE/DELETE yazan kaynak dosya = 0). **YASAKLAR TUTULDU:** Product Spec tablosu YOK · documents'a F1 durumu YOK · rota/davranış YOK. Gate 8/8: integrity_check ok · fk 0 · typecheck 4/4 · lint 0 · **519 test** (491 taban + 3 P0 + 11 v12 + 14 f1-state; server 85 · shared 295 · templates 139) · build PASS · v12 replay + tekrar-koşum idempotent (testli) · eski 494 test v12 üzerinde yeşil · `user_version=12` + kolon listeleri birebir. **app.db YENİ BASELINE: 471040 B · 2026-07-18 17:22** (önceki 430080/14:02). Rollback: `git revert <P1>` → kod geri, v12 tabloları KALIR (boş, additive-zararsız — D-48 emsali).
- [2026-07-18] **PAKET-LY2c teslim (dal `paket-ly1`; K3 GT-1 BULGU-1 düzeltmesi — GT-1 4/5, merge GT'yle kesilmişti):** **KÖK ÖLÇÜLDÜ (tahmin değil):** eski ✕ kodda VARDI ama 210px panelde satır taşması onu viewport DIŞINA itiyordu (1366px'te ✕ left=1394 → 70px dışarıda; kök div `overflow:hidden` → erişilemez) — BULGU-1'in 5 kapalı yolunun tamamı bu tek kökle açıklandı (hover tasarımı değildi). **Düzeltme:** panel 252px + sabit-en sıkı ikonlar + katman adı ellipsis (`minWidth:0` — ad kırpılır, ikon ASLA) + **GÖRÜNÜR çerçeveli 🗑** (hover-gizli DEĞİL; sönük durumlar title'lı: "Son katman silinemez" / "Kilitli katman silinemez — önce kilidi açın") + **reducer'a kilitli-katman silme guard'ı** (spec; tek-kapı — no-op → geçmişe girmez, tost çıkmaz). Spec davranışı kanıtlı: silme TEK undo girdisi (snapshot-geçmiş; 1 dispatch=1 past) — headless zincir: katman+2 şekil → 🗑 TEK adım gitti → tost → Geri-al TEK adımda İKİSİ geri → Yinele TEK adımda tekrar; 🗑 right=1343≤1366 kırpılma 0; kilitli satır sönük. +4 test (transaction · tek-girdilik · kilitli no-op · kilit-aç-sil/son-katman) → **491 yeşil** (server 71 · shared 281 · templates 139), typecheck 4/4. **MIGRATION YOK:** `user_version=11` + kolon listesi PRAGMA-teyitli DEĞİŞMEDİ (D-48 baseline geçerli). Repro belgesi doc_605069a9 KORUNDU.
- [2026-07-17] **PAKET-CD1 teslim (dal `paket-cd1`, merge kutusu Gate4 sonrası yönetişimden; PA-5):** **Creative Document v1** — CD v1 = mevcut DocumentDTO + `cd_version` damgası (yeni zorunlu içerik alanı YOK). CD1-1 shared: `CD_VERSION=1` + `cd_version: z.literal(1).default(1)` → eski belgeler/snapshot'lar parse anında MİGRATIONSIZ uyumlu (rowToDocument Zod-default deseni; DB kolonu açılmadı); +11 uyum testi (default dolum · literal/tip red · eski-snapshot · restore-partial ezilmezliği · additive tolerans · DTO alan seti). CD1-2 server: render yanıt meta'sına `cd_version` (ADDITIVE; istek şeması + kanonik imza AYNEN → **RENDER_CONTRACT_V=1 korundu**, +3 sabitlik testi). CD1-3: `docs/creative-document-v1.md` (alan tablosu · additive-only kuralı · ÇIKAR/ÇIKMAZ dış-yüzey tablosu = C1 iskeleti · aday `refs` notu [D-35: v1'de AÇILMADI, C1 ile] · K3 tek-cümle notu). **426 yeşil** (server 67 · shared 220 · templates 139), typecheck 0. **GT beyanı: ERTELENDİ** (Kurye; borç kayıtta — 12-madde liste D-34 raporunda). Kapsam-dışı teyit: refs YOK · K3 YOK · rowToDocument/PUT/restore koduna DOKUNULMADI · migration YOK · styva YOK.
- [2026-07-14] **PAKET-T1B teslim (dal `paket-t1b`, merge kutusu Fable'dan):** tur bulgusu hotfix — **FIX-3 (F1 kökü):** tekrar-intake'te aynı-etiket kategori artık MÜKERRER AÇILMAZ; `appendIntakeCategories` (shared, saf, 8 test) foldTr eşleşen projeksiyon kategorisinin ürünlerini MEVCUT kategoriye ekler (ŞERH-1/M5: mevcut satırlar aynen, yalnız ekleme; farklı not `note_dropped` raporunda — sessiz düşmez); yanıt `applied_categories`=gerçekten-yeni + `merged_into_existing[]`; özet ön-görünürlük ("N ürün mevcut 'X' kategorisine eklenecek") + onay cümlesi + sonuç satırı. **DOĞRULAMA-F2 (kod-fix'siz kapandı):** izole smoke TR müşteri+pizza → katalogda "Pizzalar" (tohum tr; "Burgers/Pizzas" kalıntısı HF3-öncesi 12 Tem ord'ları — silinecek x'te yaşar). **AUDIT:** 16 müşteri foldTr taraması — mükerrer YALNIZ kagıthane + x (ikisi de silme listesinde); **Aras/Arriva TEMİZ**. Canlı smoke 11/11 (kategori sayısı sabit + id kanıtı + order devam + CASCADE temizlik). **Kapsam dışı:** mevcut mükerrerlerin onarımı (x/kagıthane silinecek; gerçek müşteride çıkmadı), name_* çok-dil borcu, tohum içerik. 374 yeşil.

## Küçük bekleyen işler (uygulama YOK — mimar onayı bekler, M10)
- [2026-07-18] **eslint-plugin-react-hooks adayı (LY2d/D-50 backlog'u).** Lint kapısı v1 js+tseslint recommended ile kuruldu (tip-bilgisiz, plugin'siz); koddaki 2 `react-hooks/exhaustive-deps` disable-yorumu plugin'siz dünyada işlevsiz olduğundan D-50 ile kaldırıldı. Plugin kurulumu AYRI karar: kendi ilk-koşum ölçüm turuyla gelir (yeni ihlaller çıkarabilir — rules-of-hooks/exhaustive-deps); kural gevşetme/genişletme ürün sahibi kapısından.
- [x] [2026-07-09] **Glif bekçisi #18 kümesine ä/Ä genişletmesi (DE-CH kapsamı).** → UYGULANDI (commit ecc9b6b, N2-B). `GLYPH_COVERAGE` sonuna ä/Ä eklendi (`packages/shared/src/fonts.ts`); 2 birim test + tam paket yeşil (255). Repo fontları (8 dosya, Pacifico dahil) ä/Ä içeriyor → kalıcı #18 repo-font testi kırılmadı. Bekçi davranışı: özel font yüklemede HARD-BLOCK (400 missing_glyphs, eksik glif listesi) + GLOBAL (tüm müşteriler için sıkılaşır). CONSTITUTION'a dokunulmadı; rehber kapsam ibaresi "TR+FR+DE-CH" olarak güncellendi (docs).
- [2026-07-09] **İçe aktarma politikası — fiyatsız ürün.** Şu an fiyatsız satır içe aktarmada ATLANIYOR (Arriva/Schnitzelbrot katalogta hiç yok); motor boş fiyatı güvenli basar (""). İthalatın "boş fiyatla al + içe-aktarma raporunda işaretle" seçeneği mimar kararı bekler ("auf Anfrage / günün fiyatı" senaryosu). Gözlem: içe aktarma yanıtı `skipped[]` listesinde `no-price` olarak DÖNDÜRÜYOR (anlık); kalıcı/UI raporlaması ayrı — paketlenirken doğrulanacak, raporlamıyorsa rapor satırı da kapsama girer.
- [2026-07-09] **Dipnot varsayılanı — `footnote_fr` EUR-gömülü.** Varsayılan "Prix nets en euros" CHF müşteride yanlış; Almanca menüde FR dipnot ayrıca soru. Para birimi + dil duyarlı varsayılan gerekli. ä/Ä bekçi genişletmesiyle birlikte **"DE-CH yerelleştirme"** aday kümesi.
- [2026-07-09] **Marka kiti versiyonlama/geçmişi YOK — sağlamlaştırma adayı (N2 gözlemi).** Kit tek JSON kolonu, **yerinde düzenleme** (geri-al yok); v3 "swii" temizliğinde yalnız manuel dosya yedeğiyle (scratchpad) rollback güvencesi sağlandı. `brandkit_history` / sürümlü kayıt (belge snapshot geri-yükleme desenine benzer, F4-6) gelecek sağlamlaştırma adayı. Bu pakette uygulama YOK.
- [2026-07-09] **Boş QR kaynağı sessiz — uyarı adayı (N2 gözlemi).** `qrSource` bir kit alanına (ör. `instagram`) bağlıyken o alan BOŞ ise QR sessizce hiç render edilmiyor (M8-doğru: artefakt yok) AMA **uyarı da üretmiyor** — kullanıcı "instagram QR" seçtiği belgede QR'ın kaybolduğunu fark etmeyebilir. `empty-required`-benzeri bir analiz uyarısı (boş bağlı kaynak) mimar kararı bekler. Aras doc_0e0ef817'de gözlendi.

### Arriva — teslim öncesi
- [ ] **Schnitzelbrot:** fiyat gelirse KATALOĞA EKLEME işi (ürün mevcut değil — "fiyatsız duruyor" değil, hiç yok).
- [ ] **Dipnot metni:** Arriva klonunda "Prix nets en euros" teslim öncesi düzeltilecek (ZORUNLU); tercih edilen ifade müşteriye sorulabilir.

### Aras — teslim öncesi
> **Tek müşteri mesajı:** slogan + Instagram + telefon + adres, dördü birlikte teyit ettirilir. Değerler gelince ayrı küçük yazım yapılır (kit alanları doldurulur).
- [ ] **Slogan + Instagram müşteriden alınacak.** Kit v3'te "swii" bulaşması `slogan_fr` ve `contact.instagram` alanlarından temizlendi (ikisi de BOŞ bırakıldı — doğrulanmış değer elde yok, tek-kaynak disiplini: uydurulmaz). Gerçek değerler müşteriden alınıp teslim öncesi girilecek. Boş slogan render'da zarifçe düşüyor (trifold/PageChrome slot boş → hiçbir şey basmaz, M8); yine de teslimde doldurulmalı.
- [ ] **Telefon + adres teyidi (placeholder şüphesi).** `contact.phone` = "3444333444" (tekrarlı rakam), `contact.address` = "seyrantepe ispar " (İstanbul semti) bariz geliştirme artığı placeholder — Fransa'daki restorana basılırsa kaza. **Bu pakette DOKUNULMADI**; yukarıdaki tek mesajda slogan+Instagram ile birlikte müşteriye teyit ettirilecek.

## Faz S'e kayıt (mimar defteri — uygulama İLERİDE, ayrı mimar paketiyle)
- [ ] **SaaS evrimi:** Postgres'e geçiş, auth + çok kiracılı çalışma alanı (workspace), bulut depolama, render kuyruğu, abonelik. Local-first (M7) v1 ilkesi korunur; SaaS ayrı bir dağıtım hedefi olarak tasarlanacak. Şimdilik YALNIZ kayıt — kapsam kararı mimarındır.
- [ ] **Faz S ön koşulu: güvenlik yükseltme turu (mimar kararı #11):** npm audit'in major yükseltmeleri (vite 8, @fastify/static 9 vb.) SaaS'a çıkmadan ZORUNLU; v1'de yapılmama gerekçesi local-first + yalnız 127.0.0.1 dinleme.
- [2026-07-09] Kullanıcı doğrudan PSD/EPS/CDR/PDF yükleme sordu. Karar: SVG kapısı KORUNUR (fabrika bekçisi vektör-metin formatı üzerine kurulu; PSD/CDR/EPS'i doğrudan parse etmek ayrı, kırılgan bir iş olurdu). Rehbere zaten "önce SVG'ye çevir" yönlendirmesi var. Stok görseller (Freepik/Adobe Stock, PNG/JPG) zaten SORUNSUZ — SVG'ye gömülü (embed) olarak eklenir, rehber madde 3 bunu kapsıyor. Gelecekte değer görürse: PDF→SVG dönüşümünü TEZGÂH'ın kendi içinde otomatikleştirme (örn. pdf2svg benzeri araçla) düşünülebilir — bugün gerek yok, Illustrator/İnternet dönüştürücüsü yeterli.

## Faz 5'e kayıt (FAZ4-GOREV §15)
- [x] QR dijital menü statik HTML export'u. → FAZ5 §9'da uygulanıyor.
- [ ] Çoklu-yüzey sahne (tek fotoda N quad kolajı). → Faz S'e taşındı.
- [ ] Arka plan silme entegrasyonu (lokal rembg veya dış API — arşiv CONSTITUTION teknoloji ADR-7; canlı program ADR-007 değil, bkz. Canonical §0.3). → Faz S'e taşındı.
- [ ] FR açıklama önerileri (Claude API, opsiyonel/internetli). → Faz S (internetli, opsiyonel).
- [ ] AI destekli foto eşleme. → Faz S'e taşındı.
- [ ] Preset yönetim arayüzü (Faz S).

## Faz S'e kayıt (FAZ5-GOREV §11 — uygulama YOK)
- [ ] **Barındırılan dijital menü (abonelik):** Faz 5'te tek dosyalık statik HTML üretildi (§9); Faz S'te barındırılan sürüm — fiyat değişince otomatik güncellenir, temel abonelik gelir kalemi. (SaaS Vizyon Defteri'ndeki QR dijital menü ile aynı kalem.)
- [ ] **Arka plan servisi (bat'sız kalıcı çalışma):** sunucu+web `start-tezgah.bat` olmadan da PC açıkken kalıcı çalışsın — Windows servisi / tepsi (tray) uygulaması. Gerekçe: pilotta ve F5 kabul koşusunda sunucu süreci birkaç kez dışarıdan sonlandı (kırılgan başlatma); servisleştirme bunu kalıcı çözer.
- [ ] Arka plan silme / AI foto eşleme · çoklu-yüzey sahne · preset yönetim arayüzü → yukarıdaki Faz S kalemleriyle aynı (mükerrer değil).

## Faz 7 (AÇIK — öncelik Sipariş Modu; alttaki "Yol A" adayları FAZ6-GOREV §8)
- [x] **★ Sipariş Modu — ÇEKİRDEK TESLİM EDİLDİ; bu satırın "uygulama kararı mimarda" kaydı 18 gün bayat kalmıştı** (defter düzeltmesi journal `2026-07-28-taslak-yarasi-f7-defter`; ölçüm: iki keşif ajanı, kanıt hash'li). Dört vizyon bileşeni de main'de: (a) **sektör profilleri** ✓ 5 paket / 39 kategori / 140 item, 3 dil dolu (F7-B2 `06d6278`); (b) **ürün-başına sorular** ✓ kısmi — choice→varyant çalışır (F7-C `834bf4a`), boolean (`q_menu` 12 atıf) ve not soruları (`q_ek_ikram`/`q_acili`) UI'a düşmüyor, `extras[]` üretimde ölü (satır 165); (c) **içerik çipleri + öğrenme** ✓ uçtan uca canlı kanıtlı (28 Tem izole tur) — öğrenilen çip TEK DİLLİ doğar, çeviri tamamlama yüzeyi yok (P0 kuyruk); (d) **kapanış çeklisti** ✓ zorunlu kapılı — telefon/adres/website DEĞERLERİ toplanmaz (yalnız teyit kutusu), kapora/teslim yalnız-yazılır, `intake_records` okuma ucu yok (render.ts:406 kayıtlı kusur). **SINIR aynen:** atölye içi araç, POS değil. Kalan kuyruklar: bu bölümün 163/165/166/167 satırları + P0 listesi — çip çevirisi ✓ ve tüketilmeyen `skipped_bumps`/`note_dropped` ✓ kapandı (journal `2026-07-28-m8-sessizlik-cip-ceviri`); kalan: intake mutlu-yol testi · `intake_records` okuma ucu.
- [x] **Çoklu fiyat sütunu — kalem BÜYÜK ÖLÇÜDE ZATEN TAMAMDI, bayat teşhis ölçümle düzeltildi** (journal `2026-07-28-coklu-fiyat-girisi`). Eski keşif notu "eksik olan editörde giriş UI'si" diyordu; ölçüm: **CatalogPanel'de çoklu fiyat girişi Faz 1'den beri TAM** (varyant ekle/etiketle/sil — commit `2237931`), intake'te varyant-başına fiyat girişi var, veri modeli sınırsız etiketli varyant, `menu-liste-premium` `priceLayout:"columns"` FAZ1:102-104 ilanıyla birebir çalışıyor (başlıklar kategorinin ilk ürününden, mixed-variants uyarılı). Kapatılan gerçek boşluk: **editörün ItemQuickEdit'i varyant ekleyip silemiyordu** — CatalogPanel'le birebir tutarlı ekle/sil eklendi (bileşen testli). **Kalan kuyruklar (bilinçli, şerhli):** (a) import parser tek fiyat — "7,50 / 9,50" ayrıştırmaz; **ölçümlü red**: kaynak metinde etiket bilgisi yok, uydurma etiket sessiz yanlış veri; yeniden açma: gerçek müşteri yapıştırma örneği + etiket sözdizimi kararı; (b) trifold-flap ve flyer `prices[0]` sabit — bilinçli görsel sınır; çok-fiyata geçirmek görsel değişiklik = insan-kapılı ayrı paket (galeri + imza); (c) grid kolon hizası YOK ve FAZ1:88 ilanı da istemiyor ("etiket+fiyat çiftleri") — değişiklik gerekmez.
- [x] **Birim/süre alanı → KAPANDI (birim yarısı)** (journal `2026-07-28-birim-alani`). `PriceVariant.birim` yapısal alanı (default `""`, additive — ingredients emsali, migration yok) + tek tipografi kaynağı `fiyatMetni` ("/" ile başlayan bitişik: "24,00 €/kg"; diğerleri boşluklu) + fiyat basan yolların bağlanması + CatalogPanel/SlotPanel birim girişi. **Kapanan canlı yara:** intake tohumu birimi varyant ETİKETİ olarak akıtıyordu ("Kg") ve baskı yolları tek-varyant etiketini gizlediğinden kg fiyatı baskıda birimsiz görünüyordu — artık yapısal birim tek varyantta da basılır. **Bilinçli açık kalanlar:** (a) "/saat" SÜRE yarısı — süre-özel mekanizma (randevu/dk) yok; serbest metin olarak girilip basılabilir, özel destek TODO:145 "sektörsüz şablon dili"ne bağımlı (kod tabanında süre kavramı ve hizmet sektörü ölçülmedi — yok); (b) intake `q_birim` cevabı hâlâ etikete akar — birim alanına taşıma projeksiyon davranış değişikliği + "Porsiyon/Adet" soneki tartışması → ürün kararı; (c) mevcut "Kg" etiketlerinin göçü yok (sessiz mutasyon) — operatör elle taşır, UI hazır; (d) kalemin kendisi vakasız tek-cümle mimar notuydu (FAZ6 teslimi) — yorumlar ilanla verildi.
- [x] **İletişimde yapısal web alanı → KAPANDI** (journal `2026-07-28-web-alani`). `ContactSchema.website` (default `""`, additive — menu_url emsali, migration yok; brandkit JSON blob) + dört tüketici AYNI pakette bağlandı (ölü-ilan doktrini — alan okuyucusuz doğmadı): (a) BrandKitPanel iletişim girişi (`brandkit.website` etiketi); (b) QR kaynağı `"web"` — `QrSource` union + 4 manifest'in `options`'ı açıldı (F5-11 dersi AYNI pakette uygulandı: options açılmazsa paramValue kaynağı default'a düşürür; flyer bilinçli "menu"süz kaldı — `45d546f` ölçümlü karar); (c) garment satır kaynağı `"web"` + **`LINE_SOURCES` tek ilanı** — SlotPanel'in sert-kodlu kopya listesi ihraçlı sabite bağlandı, sessiz ayrışma kapandı; (d) dijital menü footer'ı (doluysa, esc'li). URL geçirimi menu_url disiplini: kullanıcı ne yazdıysa o (https öneki uydurma yok). **Bilinçli açık kalanlar:** (a) baskı şablonlarında website'in metin olarak basım hedefi TANIMSIZ — görsel/mimar kararı, bind motoru (resolvePath) hazır; (b) URL biçim doğrulaması yok (menu_url'de de yok — simetri); (c) intake'te website sorusu yok — Sipariş Modu kapsamı.
- [ ] Sektörsüz şablon dili + rehber örnekleri (restoran-dışı işler için genel terminoloji).
- [ ] TV / 16:9 ekran menü varyantı (dijital ekran çıktısı).
- [ ] Katalogdan hücreye sürükle-takas (editörde slot'a doğrudan ürün bırakma).

### Faz 7 teslim defteri — F7-A/B1/B2/C + HF1-3 + CILA1-5 + F8-A köprüsü (TAMAMI main'de; "merge onayı bekliyor" ifadeleri bayattı, hash'lerle düzeltildi — journal `2026-07-28-taslak-yarasi-f7-defter`)
- [x] **F7-A teslim** (main'e merge+push): Item.ingredients[] (IngredientRef, D1 inline denormalize + ≥1 dil refine) · Client.menu_language (fr/de) · sector.ts Zod iskeleti (İÇERİK YOK) · migration v8 (ingredient_library + menu_language kolonu) · projeksiyon (intake→katalog: varyant→prices, çip→ingredients+desc_fr, ek-ikram→desc_fr " · " ayraç, boş fiyat→pending) · K3 fiyatsız=al+işaretle + empty-price analiz uyarısı · desc_fr backfill yardımcısı · POST /api/clients opsiyonel currency+menu_language. Canlı Aras/Arriva SALT-OKUMA geriye-uyumlu; ham catalog_json hash'leri değişmedi (c65fa762/b0d68d00).
- [x] **F7-B1 teslim (main'e merge `d9fa15f`):** çip kütüphanesi + sektör paketi SERVİS uçları — GET /api/sectors (kayıt defteri, şu an []), GET /api/ingredients (SEED∪DB merge, kaynak işaretli, usage_count↓/tr sıralı), POST (öğrenme + tr dedup foldTr — SEED'e de eşleşir, learned açmaz), PATCH (çeviri tamamlama; seed çipi **kopyala-yaz override** #4, source=seed; tags kod-seed'den korunur). Tüm mantık shared saf-fn'de (merge/dedup/patch-resolve/şema), route ince tutkal. TOHUM İÇERİĞİ YOK, migration YOK. E2E smoke 4 uç (temizlikle, count tabana döndü). 312 yeşil. *(Defter şerhi: buradaki "taban" o günkü ölçümdü; bugünkü taban 0 DEĞİL — satır 164.)*
- [x] **F7-B2 teslim (main'e merge `06d6278`):** sektör/çip TOHUM İÇERİKLERİ — 60 çip (`INGREDIENT_SEED`, seed-chips.ts) + 5 paket / 39 kategori / 140 item (`SECTOR_PACKS`, sector-seed.ts): kebap_doner · pizza_fastfood · lokanta · cafe · pastane. COMMON_QUESTIONS (12) + 4 paket-özel soru (q_boy_kahve/q_boy_cay/q_adet_top/q_birim_kisi); montaj helper'ı paket sorularını item atıflarından toplar. Item adı fr=terminoloji, tr üretildi (Türkçe/loan→aynı, Fransızca→çeviri), de best-effort. **q_icerik SEED'DEN ÇIKARILDI** (#3). Referans bütünlüğü + parse testleri; canlı smoke (izole 3010): GET sectors→5, GET ingredients→60 source:seed, **SEED-OVERRIDE canlı kanıtı (B1 borcu ödendi)** + temizlik (taban 0). Mantık/route/schema DEĞİŞMEDİ. 323 yeşil. *(Defter şerhi: "taban 0" o günkü ölçümdü — bugünkü taban için satır 164.)*
- [x] **F7-C teslim (main'e merge `834bf4a`):** Sipariş Modu intake UI — mobil-öncelikli AYRI sayfa (`/siparis`, ≥44px, editör yeniden kullanılmadı) 6 adım: müşteri (yeni/mevcut) → paket ÇOKLU seçim → ürün tıkla-seç → sorular→varyant + çip düzenleme (default ön-seçili/sil/kütüphaneden-ekle/yaz→öğren; "içerik basılmasın" B4) → çeklist (zorunlu) → özet (pending+translationGaps GÖRÜNÜR M8) → COMMIT. **Atomik `POST /api/intake`** (migration v9 intake_records; tek transaction: müşteri yarat + catalog_history + projectIntake **APPEND** [ŞERH 1/M5 — mevcut değişmez] + usage_count bump [B1 #5 ÖDENDİ] + kayıt; hata→tam rollback). **Taslak localStorage** (D — müşteri yalnız commit'te; ŞERH 2 devam/at). E şeması: `SectorPackCategory.note`→`Category.note_fr` (2 tohum notu geri yazıldı — "Servies avec frites et salade" / "Change chaque jour"). planUsageBump skipped[] (ŞERH 4). 335 yeşil; typecheck temiz. **Fonksiyonel E2E** (izole 3010, UI commit boru hattıyla birebir): öğren-çip→intake→3 kat katalog→menu-liste-premium RENDER→usage bump→TAM temizlik (taban 0, gerçek müşteriler dokunulmadı) 14/14 ✓. **GÖRSEL EKRAN GÖRÜNTÜLERİ (A4 önizleme/390px mobil/özet — KABUL #2-4) o gün ÜRETİLEMEDİ (Chrome bağlı değildi) → 28 Tem'de izole ortam canlı turuyla ÜÇÜ DE üretildi, durum satır 161'de.**
- [x] **PAKET-HF1 teslim (main'e merge `0554ca9`):** F7-C görsel-QA denemesinde çıkan **kırılgan-server 4. tekrarının kök nedeni giderildi** — `apps/server` `dev` script'i `tsx watch`'tan (bazen `app.listen`'e ulaşmadan askıya giriyordu) **watch'suz `tsx`'e** çevrildi (kanıtlanmış sağlam boot: `npm run dev -w apps/server` doğrudan çalıştırılıp bat'takiyle birebir aynı health-check döngüsü **1. saniyede OK** verdi). Eski davranış `dev:watch` olarak korundu (opsiyonel hot-reload). `start-tezgah.bat` KOD OLARAK değişmedi (zaten gerçek `/api/health` 200 gate'i vardı, yanlış-OK üretmiyordu — zincirin ucu düzelince otomatik iyileşti). **Intake fetch-hata görünürlüğü (M8):** `/siparis` sectors/ingredients sorguları artık sunucu erişilemezken SESSİZ BOŞ kalmıyor — `FetchError` bileşeni (mesaj + tekrar-dene) `PacksStep` + `IntakeProductsStep`'te; network-red zinciri (sunucu kapalıyken `fetch failed`) doğrudan kanıtlandı. 335 yeşil, typecheck temiz. app.db'ye dokunulmadı (WAL kozmetik, elleşilmedi).
- [x] **PAKET-HF2 teslim (main'e merge `97da4b5`):** kullanıcı QA bulguları B1 (ürün butonları boş) + B2 (foto politikası butonları boş → İleri kilitli) **KÖK NEDENİ KANITLANIP DÜZELTİLDİ** — global `button{color:#fff}` (styles.css:40-44) `.intake-choice`'ın `background:#fff` ile çakışıp beyaz-üstü-beyaz üretiyordu (`.intake-choice` hiç `color` tanımlamıyordu); `color: var(--c-ink)` eklendi. Tek CSS satırı 4 kullanım yerini kapsar: ürün seç (B1) · foto politikası (B2) · mevcut müşteri seç · paket seç (son ikisi henüz rapor edilmemiş ama aynı kök nedenden etkileniyordu). Küçük: kategori başlığı "Tacos· ..." bitişikliği — flex container'da ayrık yalnız-boşluk text node'un CSS Flexbox spec'i gereği layout'tan düşmesiydi (`IntakeProductsStep.tsx`), boşluk `<span>` içine taşındı. `ready` mantığı (İleri kilidi) mantık-düzeyinde doğrulandı — sorun CSS'teydi, state mantığı zaten doğruydu. 335 yeşil, typecheck temiz. `.intake-chip` aynı riski taşıyor (düşük kontrast, SAF beyaz değil) — kullanıcı rapor etmedi, kapsam dışı bırakıldı.
- [x] **PAKET-HF3 teslim (main'e merge `c563c91`) — DEFTERDEN DÜŞMÜŞTÜ, kayıt geriye dönük eklendi** (journal `2026-07-28-taslak-yarasi-f7-defter`): mevcut-müşteri commit'inde dil karışımı — `intakeStore.menuLang()` mevcut müşteride sabit `"fr"` dönüyordu (adlar Fransızca inip çipler Türkçe kalıyordu); `existingClientLang` persisted alanı + ClientStep'in ClientDTO fetch'iyle menü dili gerçek müşteriden gelir. İstemci-sınırlı, `SiparisPage.tsx` + `intakeStore.ts`. Saha kanıtı: x'in Türkçe kategorileri (TUR-FIX bonus notu).
- [x] **DUR-SOR ÇÖZÜLDÜ — HF2-B, mimar kararı SEÇENEK 1 (istemci-sınırlı):** `IntakeProduct.name/category_name/category_note` artık ham `LocalizedName {tr,fr,de}` (`intakeStore.ts`) — görüntüleme HER YERDE `pickDisplay()` (HER ZAMAN TR, 8 çağrı: `IntakeProductsStep.tsx:88,102,155,156,182,256` · `IntakeSummaryStep.tsx:60`), commit eşlemesi TEK NOKTADA `pickML(x, menu_language)` (3 çağrı, hepsi `IntakeSummaryStep.tsx:38,39,40`). Sunucu/projeksiyon/IntakeAnswers şekli DEĞİŞMEDİ. Taslak sürüm bekçisi eklendi: `SCHEMA_VERSION=1` + persist `migrate()` — eski (string şemalı) taslak MİGRASYON DENENMEDEN temiz atılır + `consumeDraftDiscardedNotice()` ile operatöre açık mesaj (`SiparisPage.tsx`, sessiz kayıp yok). `.intake-chip` de `color` aldı (aynı ailenin düşük-kontrast kuzeni). 335 yeşil, typecheck EXIT=0.
  - **Kapsam dışı bırakıldı (not):** özet ekranındaki `translationGaps` listesi (item/label) hâlâ ÇIKTI dilinde gösteriliyor, TR'ye çevrilmedi — bu liste zaten "çeviri eksik, X dilinden alındı" diyen dil-duyarlı teknik bir uyarı; TR'ye çevirmek isim-bazlı eşleştirme (kırılgan) gerektirirdi. Gerekirse ayrı küçük iş.
- [x] **PAKET-CILA1 teslim (main'e merge `96f52d1`):** QA-2 dersleri (küçük cila) — (1) ürün seçim satırında varsayılan içerik önizlemesi (TR, soluk küçük yazı, `pickDisplay`); (2) **mevcut+dolu müşteride commit ONAY sorusu** (`window.confirm`, proje deseni — "bu gecenin kazası"nı önler, YALNIZ `catalogFull` yolunda tetiklenir, kod-kanıt satır-referanslı raporda); (3) müşteri adımı varsayılanı **YENİ** (`clientMode` INITIAL artık `"new"`; yan-etki: `hasDraft()`'taki `clientMode!==null` kontrolü artık anlamsız olacağından `existingClientId!==null`'a çevrildi — aksi halde her açılışta yanlış taslak-uyarısı çıkardı) + mevcut-seçili görünür bant; (4) kategori başlığı "·" boşluğu **çift-güvenceli** (HF2-1'deki span-içi düzeltme kod incelemesinde DOĞRU bulundu, yine de CSS `gap` ile ikinci bağımsız katman eklendi — kullanıcı raporu muhtemelen eski build/cache). 335 yeşil, typecheck temiz. Görsel final kullanıcının merge-sonrası turunda.
- [x] **PAKET-CILA2 teslim (main'e merge `6b02aaf`):** QA-3 bulguları — (1) **B1 BLOKER: commit sonuç ekranı** artık reset'e dayanıklı (kök neden: result state adım-6 bileşenindeydi, `s.reset()` step'i 1'e çekince bileşen unmount oluyordu → `IntakeResultData` `onCommitted` ile SiparisPage yerel durumuna taşındı; render önceliği result > taslak > adımlar; MENÜYÜ ÖNİZLE belge varsa yeniden kullanır → `/print/:id?variant=preview`; taslak reset'i commit anında KALDI — mimar onaylı, çift-commit önlemi); (2) B2 mükerrer ürün engeli (sonradan CILA3 yapısal çözümüyle değişti); (3) B3 adım-4 başlığı. 335 yeşil.
- [x] **PAKET-CILA3 teslim (main'e merge `c8c9794`):** **TEK-MODÜL intake yüzeyi (kullanıcı UX kararı — ürünün kalbi):** iki-bölge deseni ("yukarıda seç, aşağıda ayarla") KALKTI — ürün satırı 3 durumlu akordeon (SADE "+ ad"+önizleme → tık=ekle+YERİNDE aç · SEÇİLİ+AÇIK ✓+editör: varyant fiyatları/çipler/içerik-anahtarı/Kaldır · SEÇİLİ+KAPALI ✓+canlı çip önizlemesi). **Mükerrer ekleme YAPISAL imkânsız** (tek ekleme yolu sade-satır tıkı; CILA2-B2 flash/scroll öldü). **Akış 5 adım** (Sorular adımı kalktı): Müşteri·Paketler·Ürünler·Çeklist·Özet; `SCHEMA_VERSION` 1→2 (eski taslak bekçiyle atılır, "uyumsuz — temiz başlatıldı" mesajı; kullanıcı bilgilendirildi, test taslağıydı). **Kaldır güvenliği (mimar şerhi):** fiyat girilmiş VEYA çip seti varsayılandan farklıysa `window.confirm`; verisizse onaysız. **Yetim grubu (M8):** paketi kapatılmış ürünler aynı satır bileşeniyle listede kalır (görünmez ürün commit'e gidemez). `IntakeProduct.question_ids` kalktı. 335 yeşil, typecheck temiz.
- [ ] **F7-C GÖRSEL KABUL BORCU — KANIT ÜRETİLDİ (28 Tem), KULLANICI ONAYI BEKLİYOR:** izole ortam canlı turu (temiz DB, gerçek Chromium, 390px) üç kabul kanıtını da üretti — **#2 A4 önizleme** (NOTRE CARTE + fiyat + öğrenilen çip baskıda) · **#3 mobil akış** (5 adım uçtan uca, akordeon, commit) · **#4 özet** (pending + translationGaps ikisi de görünür); galeri artifact'ı kullanıcıya sunuldu (oturum kaydı). Onay/ret kullanıcının — beğenirse satır kapanır. **Tur bulgusu:** yeni görüşmede ilk harfte taslak-sorusu formu kesiyordu — AYNI PAKETTE düzeltildi (aşağıda). Eski tarif (start-tezgah.bat ile manuel tur) geçerliliğini korur: CILA3 sonrası akış **5 adım**: müşteri→paket→**ürünler (tek yüzey: satıra tık=ekle+yerinde düzenle, ✓/daralt-genişlet, Kaldır onayı)**→çeklist→özet→commit→**sonuç ekranı (CILA2-B1)**→**tek tık A4 önizleme** + **~390px mobil (akordeon, ≥44px)** + **özet pending/gaps**. `menu_language=de` ile test edilse de operatör ekranları TR kalmalı — bunu da doğrula. İlk açılışta "eski taslak atıldı" mesajı BEKLENEN (v1→v2). Bulgu çıkarsa hotfix paketi. **CILA4 eki:** F8-A'nın kullanıcı kapısı bu turda AÇILDI (round-5 A4 + tek-yüzey kanıtı geldi); yeni QA yüzeyi = kategoriye **+ özel ürün** (pizza kategorisine özel pizza → Ø deseni devralır → A4'te görünür) + müşteri adımında **Türkçe menü dili** (EK-1).
- [x] **PAKET-CILA4 + EK-1 teslim (main'e merge `b0f91e3`):** (CILA4) pakette OLMAYAN ürün kategorisinin İÇİNDE eklenebilir — her kategori sonunda "+ özel ürün" (ad+Enter → CILA3 `ProductRow` seçili+açık doğar); varyant deseni kategoriden devralınır (`deriveCategoryVariantPattern` en-sık variant sorusu, Pizzalar→Ø24/Ø32/Ø40; desen yoksa seul); ad tek-input tr slotuna (fallback'le her dilde basar, çeviri zorlaması yok); öğrenme YOK (yalnız bu görüşme kataloğu); mükerrer koruması (seed-çakışma foldTr→seed'i seç; özel-çakışma→genişlet); orphan mantığı kategori-tabanına refactor (seed-parite korundu); projeksiyon/commit/SCHEMA(2) DEĞİŞMEDİ. (EK-1) menü diline **Türkçe** — `MenuLanguageSchema` +tr, `resolveChip`/`pickML` tr dalı (tr→fr→de), `intake.ts:92` tr geçişi, web tip/select/api. 337 yeşil (shared +2 tr projeksiyon testi), typecheck temiz.
- [x] **M8 sessizlik onarımı + çip çeviri yüzeyi (journal `2026-07-28-m8-sessizlik-cip-ceviri`):** iki P0 kapandı — (1) sunucunun üç "sessiz değil" alanı (`skipped_bumps` ŞERH 4 · `merged_into_existing[].note_dropped` M8 · `catalog_had_categories` ŞERH 1) İLK KEZ tüketiliyor: commit SONUÇ ekranı atlanan çip sayaçlarını ve düşen kategori notlarını (not METNİYLE — bilgi kaybetme) uyarı bloğunda, katalog-doluluğu bilgi satırında basar; boş/false'ta sıfır ek DOM. (2) `PATCH /api/ingredients`'ın İLK tüketicisi doğdu: **Ayarlar → Malzemeler** (`/settings/ingredients`) — fr/de'si eksik çipleri listeler, satır başına tamamlar (yalnız dolu alan gönderilir; iki alan boşken Kaydet kilitli — ŞERH 3 simetrisi); `translationGaps` artık kapatılabilir. Bilinçli dar: kütüphane YÖNETİMİ değil (sil/birleştir/etiket/bayat-override → TODO:166 ayrı); intake akışında çeviri sorulmaz (görüşme bölünmez — akış kararı). **Kalan P0:** intake mutlu-yol sunucu testi + `intake_records` okuma ucu (C paketi).
- [x] **Taslak-kesme yarası düzeltildi (journal `2026-07-28-taslak-yarasi-f7-defter`):** yeni görüşmede işletme adının İLK HARFİ yazılınca form "Yarım bir görüşme var" ekranıyla değişiyordu (izole canlı turda iki kez deterministik; kök: `draftAck` sabit `false` başlarken CILA1/3'ün `hasDraft()`'a eklediği `newClient.name` koşulu). Sözleşme netleşti: **taslak sorusu MOUNT ANI sorusudur** — mount'ta taslak yoksa `draftAck` true başlar (lazy init), yazarken soru asla belirmez; önceden taslak varsa ŞERH 2 (devam/at) aynen. `SiparisPage.test.tsx` doğdu (ilk SiparisPage testi, kırmızı-kanıtlı: yaralı kodda yalnız regresyon testi kızarır).
- [ ] **CILA4 devam adayları:** (1) **özel KATEGORİ ekleme** — bugün yalnız mevcut kategori İÇİNE özel ürün; tümüyle yeni kategori (pakette olmayan) ekleme ayrı iş. (2) **sektör paketi öğrenmesi — özel ürün → tohum adayı:** özel ürünler v1'de yalnız o görüşmenin kataloğuna gidiyor; sık tekrar eden özel ürünlerin `SECTOR_PACKS` tohumuna/öğrenen kütüphaneye aday gösterilmesi (çip öğrenmesi deseninin ürün-düzeyi karşılığı).
- [ ] **ÖNEMLİ taban notu (CILA1/5) — `ingredient_library` tabanı artık 0 DEĞİL:** F7-B1/B2 kapanışlarında "taban 0" varsayımıyla smoke yapılmıştı; gerçek kullanım (QA turları) sonrası DB'de **6 satır kalıcı** — hepsi `source=seed`, gerçek 60-çip kod-tabanının kullanım-sayacı kayıtları (`ing_doner_eti`, `ing_salata`, `ing_domates`, `ing_sogan`, `ing_tavuk`, `ing_patates_kizartmasi`; `usage_count` bump'larından, TEMİZLİK-QA1'de KASITLI OLARAK dokunulmadı — "usage_count artışlarına dokunma, zararsız" kararı). **Gelecek smoke/E2E turlarının "taban 0" beklentisi bu nota göre güncellenir** — doğru beklenti: "0 `learned` satır" (yeni malzeme yazılmadıysa), seed-sayaç satırları MEŞRU ve kalıcıdır.
- [ ] **F7 sonraki adaylar:** tam çok-dilli render (desc menu_language'a göre kuruluyor ama şablonlar hâlâ desc_fr'den basıyor); q_menu SEUL/MENÜ + boolean varyant sorularının UI'da ele alınışı (v1'de yalnız choice-opsiyonlu sorulardan varyant türetiliyor); intake-record görüntüleme/düzenleme; mockup (Faz 8 — çeklist ölçü/yüzey notları VERİ olarak duruyor).
- [ ] **Çip override temizliği (F7-B1 #4 bakım notu):** seed override (source=seed) her zaman kod-seed'i ezer (atölye düzeltmesi kazanır); kod-seed sonradan aynı değere güncellenirse override BAYAT kalır. İleride "kod-seed'e eşitlenen override'ı sil" bakım aracı — veri riski yok, yalnız sadeleştirme.
- [ ] **Yapısal ek-ikram alanı (D2 ertelemesi):** projeksiyon ek-ikramı desc_fr'ye görsel ayraçla (" · ") gömüyor; yapısal `item.extras[]` / `note_fr` alanı render/UI (intake) fazında değerlendirilecek — bugün desc_fr yeterli, şema şişmedi.
- [x] **Server HTTP route birim-test harness'ı — P0'DA ÖDENDİ (2026-07-18, F1 ön-şartı):** `buildApp()` (apps/server/src/app.ts) dinlemeden app döndürür → `app.inject()` ile gerçek uç testi; DB izolasyonu `TEZGAH_DB_PATH` (import anında) + `setDatabase` takas seami; rota imzaları DEĞİŞMEDİ (24 dosyaya dokunulmadı — ESM canlı bağ). Örnek: `app.test.ts` (health · izole-boş clients · 404 yolu). Not: per-rota DI bilinçli YAPILMADI (24 dosya = davranış riski); ihtiyaç doğarsa ayrı karar.

### Park notları (PAKET-F7-KEŞİF — 2026-07-10, uygulama YOK)
- [ ] **Glif bekçisi politika kararı — hard-block KALIR.** Özel font ä/Ä dahil kümeyi tam karşılamazsa yükleme reddedilir (mevcut davranış korunur). Tetikleyici: bir font YALNIZCA ä/Ä yüzünden reddedilirse, dil-duyarlı küme (müşteri diline göre gevşetme) tartışması O GÜN açılır — bugün spekülatif genişletme yok. İlgili: [[phase-6-durumu]] bekçi kararları.
- [ ] **Harness/health-check standardı: `127.0.0.1`, `localhost` değil.** Windows'ta `localhost` önce IPv6 `::1`'e çözülüyor; server yalnız IPv4 `127.0.0.1`'e bind olduğundan `localhost` üzerinden fetch/Invoke-WebRequest ECONNREFUSED veriyor (N2 kapanışında yaşandı). Tüm betik/health-check/örnek çağrılarda `127.0.0.1` kullan. (Server saha erişimi için `0.0.0.0` bind'i ayrı iş — Sipariş Modu saha maddesi.)
- [ ] **Müşteri kaydı hijyeni — gerçek/test işaretleme (KULLANICI CEVABI BEKLENİYOR).** Kayıtlı müşterilerden `ARAS Grill Lyon`, `Basel Kebap Haus`, `Antalya Kebab — Lyon` gerçek mi test mi belirsiz; kullanıcıya sorulacak, test olanlar temizlenecek/etiketlenecek. (Gerçek pilot müşteriler: Aras Restaurant, Arriva Restaurant; klon: Arriva Native N1.) **CILA1/5 eki:** QA-2 turunda `intake_commit`'in `new_client` yoluyla **iki yeni test müşterisi** daha oluştu — `"fert"` ve `"seyrantepe"` (isimler açıkça placeholder/test; "seyrantepe" F7-A'daki Aras'ın eski placeholder-adres semtiyle aynı) — bunlar da bu kaydın kapsamına girer. **Silme UI'da MEVCUT** (`ClientDetailPage` → `client.delete` butonu, `api.deleteClient`) — mimara ayrıca bildirilecek bir eksiklik yok; kullanıcı bu iki test kaydını (ve karar verildiğinde diğerlerini) doğrudan UI'dan silebilir.

## SaaS Vizyon Defteri (UYGULAMA YOK, yalnız kayıt)

> **K-1 ŞERHİ (2026-08-06, ürün sahibi kararı — `docs/URUN_SAHIBI_KARARLARI.md`):**
> bu defterdeki iki kalem artık "vizyon" sınıfında DEĞİLDİR, ticari yolun
> üzerindedir: **paket şablonları** (K-1'in "tıklayarak bitir" yarısının ana
> kaldıracı) ve **Faz S** (modülün ozalitçiye kiralanabilmesinin teknik ön
> koşulu — çok kiracılık + lisans kapısı). Sıralamayı yönetişim keser;
> burada yalnız sınıf değişikliği kayıtlıdır. Kiralama mı satış mı, fiyat ve
> lisans metni HÂLÂ ürün sahibindedir (K-1 "karara dahil olmayan" bölümü).

- [ ] **QR dijital menü:** katalogdan üretilen mobil menü sayfası. v1: statik HTML export (Faz 5 adayı). Faz S: barındırılan sürüm, fiyat değişince basılı PDF ile birlikte otomatik güncellenir — temel abonelik gelir kalemi.
- [ ] **Grid şablonuna çok sayfalı akış (flow) modu:** 80-100 kalemlik kataloglar için (Faz 4).
- [ ] **İsimden foto önerisi:** ürün adı → ortak havuzdaki etiketli stok foto önerisi; havuz varlıklarına etiket alanı (Faz 4/5).
- [ ] **Çoklu-yüzey sahne:** tek fotoğrafta birden çok quad (vitrin + duvar + ışıklı pano) → "dükkan böyle görünecek" kolaj mockup'ı (Faz 4/5).
- [ ] **AI konsept görselleştirme (Faz S):** mekan fotoğrafından yenilenmiş konsept görseli; "konsept görselleştirme" olarak konumlanır, iç mimarlık hizmeti DEĞİL. Mimar kararı: SketchUp benzeri 3B modelleme kalıcı kapsam dışı.
- [ ] **Paket şablonları:** "açılış takımı" gibi tek tıkla N kalemli proje presetleri (Faz 4).

## Faz 4'e inceleme notu (kapandı ✓ — F4-14)
- [x] **PDF sayfa ölçümünde +0.2–0.4 mm sapma:** kök neden ÖLÇÜLDÜ ve şüphelenilen pt-yuvarlama DIŞLANDI — Chromium `page.pdf` kâğıt boyutunu birimden bağımsız iç ızgarasına oturtuyor (mm/cm/px özdeş; inç yalnız yüksekliği düzeltiyor). Tutarlı ucuz düzeltme yok → tolerans kararı Değişiklik Günlüğü'nde (bleed 3mm >> sapma; kesim crop marks'tan).

## Faz 4'e kayıt (FAZ3-GOREV §9 — Faz 4'te uygulandı ✓)
- [x] **SVG import + slot işaretleme:** hazır SVG tasarımı içe al, slotları elle işaretle.
- [x] **Tema kütüphanesi ekranı:** temaları görsel galeriden seç/önizle.
- [x] **Toplu fiyat güncelleme:** katalogda yüzde/mutlak artış aracı.
- [x] **CMYK dönüşümü (Ghostscript):** matbaa PDF'i için; mevcut +0.2–0.4 mm pt→mm sapma notuyla birlikte incelenecek (yukarıdaki Faz 4 inceleme notu).
- [x] **Snapshot'tan geri yükleme:** export_records.snapshot_json → belgeyi o ana döndür.
- [x] **Zip yedek:** data/ klasörünü tek tıkla arşivle (M7).
- [x] **Parse sözlüğü yönetim arayüzü:** PRODUCT_DICT / KEY_ALIASES'ı UI'dan genişlet.
- [x] **İki kademeli broderie uyarısı (mimar kararı #8):** kural doğru ("broderie + kısa kenar < 15 cm" güçlü uyarı; bavette'te çıkmaz — kabul 6 örneği mimar hatasıydı, davranış değişmedi). Faz 4'te değerlendirilecek: her broderie belgesinde hafif bilgi notu + <15 cm alanda mevcut güçlü uyarı.
- [x] **TextLines letterSpacing birim düzeltmesi (mimar #10 — F4-2'de uygulandı; üç önce/sonra piksel diff 0.0000%):** motor letterSpacing'i birimsiz kullanıcı birimi (1 birim = 1 mm) yazar; mevcut mm-tabanlı yazar değerleri **3,7795 ile çarpılır** (em-tabanlılar dönüşüm dışı — #10 uygulama notu); katsayı/yön canlı ölçümle teyit, önce/sonra piksel kanıtı zorunlu.

## Faz 3'e kayıt (FAZ2-GOREV §10 — uygulandı ✓)
- [x] **Tişört + önlük şablonları:** `technique: impression|broderie`; broderie'de yalnız vektör + cm ölçü + iplik notu çıktısı; küçük alan + broderie'de ince-detay uyarısı.
- [x] **Mockup motoru:** quad-warp; keşif fotoğrafları sahne oldu (mockup_scenes, müşteri|ortak).
- [x] **Vitrophanie/tabela belge tipleri:** cm bazlı; découpe'ta text→path (fontkit, mimar #7).
- [x] **Tişört mockup renk seti:** mavi dahil (fabric_color seti).
- [ ] Sunum PDF'inde kampanya slotunun katalogdan canlı binding'i (Faz 2'de serbest metin + override; en basit çözüm §0.6).
- [ ] Sipariş kalemi ↔ belge durum senkronu otomasyonu düşünülebilir (Faz 2'de bilinçli olarak elle — FAZ2-GOREV §2.5).

## Faz 2'ye kayıt (FAZ1-GOREV §9 — uygulandı ✓)
- [ ] **carte-fidelite şablonu netleşti:** `stampCount=10` (2×5 dizilim), **numaralı** damga kutuları (1.–10.), üst-sağ alt başlık slotu (*"1 menu acheté = 1 tampon"*), alt tam-genişlik ödül bandı (`--c-accent` zemin, ör. *"11ᵉ KEBAB OU PIZZA OFFERT !"*); arka yüz: logo, "CARTE DE FIDÉLITÉ" başlığı, tel, adres, hizmet satırı (*Sur place · à emporter · Livraison*), saat.
- [ ] **Flyer:** 21×21 cm kare format preseti (katlamalı); teslimat bölgeleri + minimum sipariş serbest-metin slotu; **çift saat bloğu** (açılış ↔ teslimat saatleri ayrı).
- [ ] **QR online sipariş:** mevcut `delivery[]` şemasından beslenir (CONSTITUTION §10 ile birlikte).

## Faz 1 sırasında düşülen notlar
- [x] **QR slotları — MİMAR KARARI (Faz 1 kabulünde):** QR Faz 2'de kalır; Faz 2'de `qrcode`→SVG altyapısıyla birlikte **menü şablonlarına da opsiyonel slot olarak eklenir** (FAZ2-GOREV §5, mimar kararı #2). Çelişki kapandı.
- [ ] Proje yönetimi arayüzü yok; ilk belgede müşteri başına "Genel" projesi otomatik açılıyor (en basit çözüm, §0.6) → Faz 2 Sipariş Defteri bunu üstleniyor
- [x] **Dekor foto alt bandı — MİMAR KARARI (Faz 1 kabulünde):** liste şablonundaki deterministik alt bant yerleşimi **kabul edildi**; yeniden konumlama yok.
- [ ] Belgelerde ad kolonu yok (şablon adı + tarih gösteriliyor); istenirse ileride `documents.name` migration'ı
- [x] **Test müşterileri — KULLANICI KARARI:** "ARAS Grill Lyon" ve "Basel Kebap Haus" demo/test verisi olarak `data/`'da KALIR; silinmeyecek.

## Faz 0 sonrası bilinen sınırlar
- [ ] Ortak fotoğraf havuzu (assets.client_id = NULL) şemada hazır, arayüzü yok → Faz 1/2 (CONSTITUTION §15/S3)
- [ ] Müşteri klonlama endpoint'i ve arayüzü → Faz 2 (M6)
- [ ] Vitest birim testleri, binding motoruyla birlikte gelecek → Faz 1 (§12)
- [ ] Müşteri silme geri alınamaz; şimdilik confirm() ile korunuyor, ileride "arşivle" düşünülebilir
- [x] Asset silme endpoint'i (kullanım korumalı) Faz 4'te geldi (FAZ4 §11)
- [ ] npm audit: 3 zafiyet (2 orta, 1 yüksek) — @fastify/static 8.x (path traversal, GHSA-pr96-94w5-mx2h) ve vite 5/esbuild (dev server, GHSA-67mh-4wv8-2f99). Düzeltme major sürüm ister (@fastify/static 9, vite 8); sunucu yalnız 127.0.0.1 dinlediği için risk lokal-düşük → **mimar kararı #11:** fazlarda yapılmaz, "Faz S ön koşulu: güvenlik yükseltme turu" kaydına bağlandı

### Uyarı kaynağı dürüstlüğü sonrası şerhler (2026-07-26, uyarı-kaynağı paketi)
- [x] **`apps/web` TEST ALTYAPISI YOK (ölçülmüş kapı sınırı):** ~~web'in `test` script'i yoktur ve kök `npm test -ws` fan-out'u onu **sessizce atlar**~~ → **MEKANİZMA TARİFİ YANLIŞTI, ölçümle düzeltildi** (2026-07-27-web-test-altyapisi): test kapısı kök `npm run test -ws --if-present` script'ini **hiç kullanmıyor**; `gates.ts`'te **sabit kodlu** bir `TEST_WORKSPACES` listesi kullanıyor. Fark önemli, çünkü `--if-present` teşhisi yanlış tedaviye götürürdü: web'e test script'i eklemek **tek başına hiçbir şey değiştirmezdi** — kapı yine koşmazdı. Şerhin sonucu doğruydu (web düzeltmeleri ölçülemiyordu), sebebi yanlıştı. Ele alan paket: `2026-07-27-web-test-altyapisi`.
- [ ] **Uyarı köprüsünü el yazımı ailelere uzatmak ÖLÇÜMLE REDDEDİLDİ:** `TemplateEntry.warnings` 11 ailenin yalnız 1'inde (fabrika) tanımlı; köprüsüz ailelerde uyarı **kaybolmuyor** — `analyzeDoc` onları ADLI switch dalından çağırıyor. Köprüyü 10 aileye uzatmak **sıfır davranış** değiştirir (switch id'yi önce yakalar) = süs alan. Yeniden değerlendirme koşulu: `analyzeDoc`'un adlı switch'i kaldırılmak istenirse — o zaman `pages` kaybı (`entry.pageCount` **10/11** id'de ilanlı — ölçüldü; eksik olan tek id `kabul-fabrika`) ve çift-analiz maliyeti birlikte ölçülmeli.
- [ ] **Prototipsiz fabrika şablonuna analiz üretmek — ürün sorusu:** emitter `analyze.ts`'i yalnız prototipli dalda üretir; prototipsiz şablonun uyarı kaynağı YOKTUR ve artık dürüstçe **boş** döner (eskiden alakasız grid uyarıları görünürdü). Prototipsiz şablonda kapasite/taşma matematiğinin ne olacağı ayrı karardır.
### Param geçerli-değer ilanı sonrası şerhler (2026-07-27)
- [x] **Sunucu belge `params`'ını HİÇ doğrulamıyor → KAPANDI** (journal `2026-07-27-sunucu-params-dogrulamasi`). İki mekanizma düzeltmesi: uç `PATCH` değil **PUT**'tur ve params içerikte **REPLACE** semantiği taşır (sunucu her PUT'ta tamamını görür → tam-şema kapısı mümkün); "ZodError yakalanmıyor → 500" tarifi de eksikti — gerçek zincir *tarayıcıda* parse → ErrorBoundary yok → `__PRINT_READY__` atanmaz → **Puppeteer 30-45sn timeout** → 500 `{error:"internal"}` (ZodError mesajı istemciye hiç ulaşmaz). Geçiş kararı **REDDET** (yazma sınırında 400): üretim DB'si bu konteynerden ölçülemedi (yerel DB **boş** — 17 tablo 0 satır; sayım hattı sentetik fixture'la 9/9 doğrulandı ama "üretimde ihlal yok" iddiasına izin vermez), karar güvenli-yön analiziyle alındı — yazma-sınırı reddi mevcut veriyi tuğlalamaz (okuma gevşek kalır), KIRP sessiz veri mutasyonu olurdu, UYAR yarayı açık bırakırdı.
- [ ] **İLAN ↔ ŞEMA sınır ayrışması (ölçüldü, güvenli yönde):** `enseigne h_cm` manifest ilanı **20–500**, uygulayan Zod `positive().max(2000)` → `h_cm: 1800` ilanı ihlal eder, şemadan geçer, çizilir. Vitro'da `min: 10` ilanı da uygulanmıyor (`w_cm: 1` geçer). Bu paket **tehlikeli yönü** (ilanın şemadan gevşek olması) nöbetçiyle kapattı; ilanı fiilen uygulamak Zod'u sıkılaştırmayı gerektirir. **Not (sunucu-params paketi sonrası):** sunucu kapısı MEVCUT Zod'u uygular — yani `h_cm: 1800` sunucudan hâlâ geçer (Zod ≤2000), yalnız ilan (≤500) ihlal edilmiş olur. Zod'u ilana çekmek ayrı bir daraltmadır ve kayıtlı-veri ölçümü ister; **üretim DB'sine erişilen bir ortamda** `scratchpad` prob deseni (sunucu-params keşfi) yeniden koşulmalı.
- [x] **`fabric_color` birliği + sahne↔belge eşleşmesi → KAPANDI** (journal `2026-07-27-kumas-rengi-birligi`). Birlik tek yerde ilan edildi (`@tezgah/shared` `KUMAS_RENKLERI` + `kumasRengiHex`/`kumasRengiEsit`); `sahneSkoru` karşılaştırması çevirmene bağlandı → sahne "white" ile belge "#ffffff" artık **eşleşiyor** (manifest'in `eslesme_parami` ilanı UI yolunda ilk kez canlı); garment `FABRIC_HEX` yerel kopyası ve ScenesPanel sert listesi ilandan okunuyor. **Bilerek açık kalanlar:** (1) `GarmentParamsSchema` gevşek kaldı — sıkılaştırma okuma yolunda eski "siyah"lı belgeyi çökertirdi; yeniden açma: üretim DB sayımı; (2) "tanınmayan renk" uyarısı **ölçümlü red** — LayoutWarning genişletme maliyeti + yol UI'dan erişilemez (color input hep geçerli hex, sahne select kapalı küme); yeniden açma: üretim DB'de tanımsız değer ölçülürse; (3) çizici (`fabricToHex`) `#`-passthrough toleransı korundu — sıkılaştırma render çıktısını değiştirir, ayrı karar.
- [ ] **Depoda ÜÇ farklı "geçerli hex" tanımı:** `HexColor` (`schemas.ts:10`, katı 6 hane, brandkit + `cut_color`), `ThemeHex` (`schemas.ts:335`, 6 **veya** 8 hane, export edilmiyor, tema token'ları), ve `engine/qr.ts:48` (`#` **isteğe bağlı**, 6 haneden fazlasını sessizce kırpar, eşleşmezse luminans **0 = koyu** varsayar). Üçünü tek tanıma indirmek ayrı tur; `paramValue` bu pakette **kanonik olana** bağlandı.
- [ ] **Doğrulanmayan diğer renk alanları:** `SceneOverlaySchema.color` ve sahne `fabric_color` düz `z.string()`. Yerleşik temalar (`themes.ts`) düz TS sabitleri — Zod'dan hiç geçmiyor.
- [ ] **Garment koşullu gerekliliği: 3 ön koşuldan 1'i kapandı.** `paramValue` dejenerasyonu (3. koşul) bu pakette kapatıldı. Kalan ikisi açık: (1) `areas` paramı `manifest.params`'ta YOK — EditorPage sert-kodlu panelden yazıyor; (2) `area:*:logo` slotları `manifest.slots`'ta YOK — motor garment'ın gerçek slotlarını adlandıramıyor.

### Web test altyapısı sonrası şerhler (2026-07-27)
- [x] **`apps/web`'de BİLEŞEN testi YOK → ALTYAPI KURULDU** (journal `2026-07-28-web-bilesen-testleri`). jsdom ^27.4.0 (+ @testing-library/react ^16.3.2) **per-dosya pragma** ile — global config değişmedi, saf testler node'da kalıyor (sıfır-konfig deseni korundu). Üç pakettir şerhlenen "koşarak doğrulanamadı" zincirleri artık koşuluyor: sayı-input boş girdi guard'ı, save-400→saveError gösterimi, startDesign geri sarması. **Ölçüm düzeltmesi:** "EditorPage render edilemez (864 satır)" varsayımı yanlıştı — react-konva import etmiyor (konva yalnız AtolyePage'de), komple render jsdom'da mümkün. **Hâlâ açık:** kapsam DOLDURULMADI (30 bileşen/sayfa; bu paket altyapı + 3 zincir); AtolyePage (konva/canvas) jsdom'da test edilemez; sürükleme/`getBoundingClientRect` akışları jsdom sınırı.
- [ ] **`TEST_WORKSPACES` sabit kodlu kalır — bu bir KARAR:** liste runtime'da `package.json`'lardan türetilebilirdi; türetilmedi. Türetseydik kapı, ölçtüğü şeyin sınırını her koşumda kendi keşfeder ve denetim defterine yazdığı "şu workspace'ler koşuldu" iddiası sessizce kayardı. Kapsam bir **ilandır**; ilanı diske sormak kapıyı kırılganlaştırır. Bedeli `kapi-kapsami.test.ts` nöbetçisiyle ödenir. Yeniden değerlendirme koşulu: workspace sayısı elle sürdürülemeyecek kadar artarsa (bugün 5).
- [ ] **`analyzeDoc` düzeltmesi BUGÜN SIFIR DAVRANIŞ değiştirir (kendi bulgum, F1-latent-yara-iddiasi):** karşıt okuma ölçtü — default dalına düşen tek id `kabul-fabrika`'dır (11 id'nin 10'u adlı switch dalında yakalanır) ve o **köprülüdür**, `pageCount` de ilan etmez. Yani hem kaldırılan `analyzeGrid` fallback'i hem `pageCount` okuması **latent** ayrışma kapatır, ölçülmüş vaka değil. İlk taslakta kod yorumunu ve journal notunu yarayı **canlıymış gibi** yazmıştım; düzeltildi (yorum + nöbetçi başlığı + seq=18 notu). Ders: "erişilebilir kod yolu" ile "bugün erişilen kod yolu" ayrı şeylerdir ve paket yorumu bunları ayırmak zorundadır. Yara **canlılaşma koşulu**: emitter'ın prototipsiz dalı ilk kez çalıştığında (pin `factory.test.ts:123`) veya emitter `pageCount` emit ettiğinde.
  - **DEVAMI (2026-07-27):** aynı sınıf ayrışmanın **ADLI dalda CANLI hâli** bulundu — `menu-grid-cells` `pages: 1` sabiti döndürüyordu, ilan (`entry.pageCount`) ise `flow="multipage"`de 9 diyordu. Kullanıcı etkisi: `EditorPage` sayfa seçicisini `pages > 1` ile gösterir → 9 sayfalık menüde operatör 2–9. sayfalara **hiç geçemiyordu**, baskı ise 9 sayfa basıyordu. **Ders:** bir sınıf hatayı bir dalda kapatmak, kardeş dalların ölçüldüğü anlamına gelmez — latent teşhisi doğruydu ama *kapsamı* eksikti. Düzeltildi + kayıt-geneli nöbetçi kuruldu (11 id × 2 param kümesi, ayrışan 0). Kusuru bulan şey, `apps/web`'i ilk kez ölçebilen kapının kendisiydi.
